!!!
%html{lang: "en"}
  :javascript
    (function(){var t=localStorage.getItem('theme')||'auto';var r=(t==='auto'?(window.matchMedia('(prefers-color-scheme:light)').matches?'light':'auto'):t);document.documentElement.setAttribute('data-theme',r);})();
  %head
    %meta{charset: "UTF-8"}
    %meta{name: "viewport", content: "width=device-width, initial-scale=1"}
    %title= "SVXReflector #{@reflector_host} · Reflector Config"
    = csrf_meta_tags
    %script{src: "/vendor/tailwind/tailwind.js"}
    :javascript
      tailwind.config = {
        theme: {
          extend: {
            colors: {
              ghbg: '#0d1117',
              ghcard: '#161b22',
              ghborder: '#30363d',
              ghborder2: '#21262d',
              ghtext: '#c9d1d9',
              ghmuted: '#8b949e',
              ghdim: '#6e7681',
            }
          }
        }
      }
    %link{rel: "stylesheet", href: "/vendor/bootstrap-icons/font/bootstrap-icons.min.css"}
    :css
      body { background: #0d1117; }
      .accordion-body { max-height: 0; overflow: hidden; padding-top: 0; padding-bottom: 0; transition: max-height 0.3s ease, padding 0.3s ease; }
      .accordion-body.open { padding-top: 1rem; padding-bottom: 1rem; }
      .accordion-chevron { transition: transform 0.3s ease; }
      .accordion-chevron.open { transform: rotate(180deg); }

  -# Help texts from official svxreflector.conf(5) man page
  - help = {}
  - help["LISTEN_PORT"] = "The TCP and UDP port number to use for network communications. The default is 5300. Make sure to open this port for incoming traffic to the server on both TCP and UDP. Clients do not have to open any ports in their firewalls."
  - help["HTTP_SRV_PORT"] = "Set which port to use for the HTTP server. The HTTP server can be used to fetch reflector status. No port is set by default. Don't expose this port to the public Internet. The built in web server is very simple and could be incompatible with some clients. It has not been audited for security problems so there may be security issues. There is also the risk of some client overwhelming the reflector with requests causing disturbances in the reflector operation. Example: HTTP_SRV_PORT=8080"
  - help["CODECS"] = "A comma separated list of allowed codecs. For the moment only one codec can be specified. Choose from the following codecs: OPUS, SPEEX, GSM, S16 (uncompressed signed 16 bit), RAW (uncompressed 32 bit floats). The default is OPUS and you should have a very good reason for changing this since that codec provide both low bandwidth (~20kbps by default) and very good audio quality."
  - help["ACCEPT_CALLSIGN"] = "A regular expression for verifying that a received login callsign has the correct format. The default is to roughly match standard ham radio callsigns. Example: ACCEPT_CALLSIGN=\"S[A-M]\\d[A-Z]{1,3}(?:-[A-Z0-9]{1,3})?\""
  - help["REJECT_CALLSIGN"] = "A regular expression for rejecting login callsigns. This can be used to block some specific callsigns or even a whole range of callsigns. No callsigns are rejected by default. Example: REJECT_CALLSIGN=\"SM0XYZ|SM1ABC|SM2.*\""
  - help["ACCEPT_CERT_EMAIL"] = "A regular expression for verifying that email addresses in a received certificate signing requests are valid. If unset, which is the default, all email addresses are accepted. If an invalid email address is received from a node it will be disconnected and an error message will be sent to the node."
  - help["SQL_TIMEOUT"] = "Use this configuration variable to set a time in seconds after which a clients audio is blocked if he has been talking for too long. The default is 0 (disabled)."
  - help["SQL_TIMEOUT_BLOCKTIME"] = "If a client has been talking for longer than the time specified in the SQL_TIMEOUT configuration variable he will not be able to talk again until the time in this configuration variable have elapsed. If not specified, the default is one second."
  - help["TG_FOR_V1_CLIENTS"] = "Set which talk group to place protocol version 1 clients in. Without this configuration version 1 clients will not be able to use the reflector since they cannot select a talk group by themselves. Talk groups should normally be configured to start with the ITU-T E.212 Mobile Country Code (MCC) for your country, e.g. 240 for Sweden."
  - help["RANDOM_QSY_RANGE"] = "Specify in which talk group range the reflector server should select random talk groups used when using the QSY functionality. The range is specified using two colon separated values. The first one is the lower limit and the second value is the number of talk groups in the range. As an example, if set to 1239900:100 QSY talk groups will be selected from the range 1239900 to 1239999. The recommended lower range to use is <MCC>9900 where MCC is the ITU-T E.212 Mobile Country Code for your country."
  - help["TIMESTAMP_FORMAT"] = "This variable specifies the format of the timestamp that is written in front of each row in the log file. The format string is in the same format as specified in the strftime(3) manual page. The default is \"%c\" which is described as: \"the preferred date and time representation for the current locale\". Example: TIMESTAMP_FORMAT=\"%d %b %Y %H:%M:%S\" would give a timestamp looking something like: \"29 Nov 2005 22:31:59\"."
  - help["LOG"] = "GeuReflector: Per-subsystem log verbosity. Comma-separated <subsystem>=<level> pairs; * is the wildcard. Subsystems: trunk, satellite, twin, client, mqtt, redis, core. Levels: trace, debug, info, warn, error, off. Default: *=info. Example: LOG=*=warn,trunk=debug,mqtt=info."
  - help["COMMAND_PTY"] = "Configure a path for a pseudo tty device to send runtime commands to the svxreflector. Example: COMMAND_PTY=/dev/shm/reflector_ctrl. To change a config variable: echo CFG GLOBAL SQL_TIMEOUT_BLOCKTIME 60 > /dev/shm/reflector_ctrl. To sign a client certificate: echo CA SIGN callsign > /dev/shm/reflector_ctrl."
  - help["CFG_DIR"] = "Specify the path to a directory that contain additional configuration files. If a relative path is specified, the path will be relative to the directory where the main configuration file is at. All files in the specified directory will be read as additional configuration. Filenames starting with a dot are ignored."
  - help["CERT_PKI_DIR"] = "The path to the directory containing PKI (Public Key Infrastructure) files. If a relative path is given, the value of the build time variable SVX_LOCAL_STATE_DIR (e.g. /var/lib/svxlink/) will be prepended. Default: pki/"
  - help["CERT_CA_KEYS_DIR"] = "The path to the directory containing private key files. If a relative path is given, the value of the CERT_PKI_DIR variable will be prepended. Default: private/"
  - help["CERT_CA_PENDING_CSRS_DIR"] = "The path to the directory containing unsigned CSR files. If a relative path is given, the value of the CERT_PKI_DIR variable will be prepended. Default: pending_csrs/"
  - help["CERT_CA_CSRS_DIR"] = "The path to the directory containing signed CSR files. If a relative path is given, the value of the CERT_PKI_DIR variable will be prepended. Default: csrs/"
  - help["CERT_CA_CERTS_DIR"] = "The path to the directory containing signed certificate files. If a relative path is given, the value of the CERT_PKI_DIR variable will be prepended. Default: certs/"
  - help["CERT_CA_HOOK"] = "Set to a path for an external application that is run on CA events like when a CSR is received or when a certificate is signed. The following environment variables will be set up before running the external application: CA_OP (set to PENDING_CSR_CREATE, PENDING_CSR_UPDATE or CSR_SIGNED), CA_CSR_PEM (CSR data in PEM format, not set for CSR_SIGNED), CA_CRT_PEM (Certificate data in PEM format, only for CSR_SIGNED)."
  - help["COMMON_NAME"] = "The common name (CN) used in the subject of the certificate."
  - help["ORG_UNIT"] = "The organizational unit (OU) used in the subject of the certificate. Example: ORG_UNIT=\"VHF/UHF-sektionen\""
  - help["ORG"] = "The organization name (O) used in the subject of the certificate. Example: ORG=SSA"
  - help["LOCALITY"] = "The locality name (L) used in the subject of the certificate. Example: LOCALITY=Boden"
  - help["STATE"] = "The name of the state or province (ST) used in the subject of the certificate. Example: STATE=Norrbotten"
  - help["COUNTRY"] = "The ISO country code (C) used in the subject of the certificate. Example: COUNTRY=SE"
  - help["EMAIL_ADDRESS"] = "The email address that can be used to get in contact with the owner of the certificate. Example: EMAIL_ADDRESS=sysop@svxlink.example.org"
  - help["SUBJECT_ALT_NAME"] = "A comma separated list of subject alternative names. Example: SUBJECT_ALT_NAME=DNS:public-hostname.example.org,IP:172.17.1.42"
  - help["TRUNK_SECTION_NAME"] = "Enter a short identifier for this trunk peer (e.g. REFA_REFB). The TRUNK_ prefix is added automatically to form the config section name (e.g. [TRUNK_REFA_REFB]). The section name must be identical on both sides of the trunk link. By convention, put the two reflector identifiers in alphabetical order — REFA_REFB, not REFB_REFA."
  - help["ALLOW"] = "A regular expression that must match the whole callsign of the nodes that try to select this talkgroup. The regular expression standard used is the same as for ECMAScript with the exception that the backslash special patterns (e.g. \\s, \\d etc) must be expressed with double backslash notation. An example that match most Swedish callsigns and a specific Norwegian one is \"S[A-M]\\\\d.*|LA8PV\"."
  - help["AUTO_QSY_AFTER"] = "Set this to the number of seconds after which an automatic QSY to a random talkgroup is requested. This is typically used to keep call channels and wide area channels clear so that stations does not dwell there for too long. Auto QSY is only triggered directly after a talker stop event. The default is that auto QSY is disabled (AUTO_QSY_AFTER=0)."
  - help["SHOW_ACTIVITY"] = "If set to 0, do not indicate in the HTTP status message when the talkgroup is in use by a node. Default is 1 = show activity."
  - help["ALLOW_MONITOR"] = "A regular expression that must match the callsign of nodes allowed to monitor this talkgroup. Uses the same regex format as ALLOW."
  - help["LOCAL_PREFIX"] = "GeuReflector: A comma-separated list of TG number prefixes that this reflector owns. Talkgroups whose number starts with one of these prefixes belong to this reflector. Example: LOCAL_PREFIX=1 means this reflector owns TGs 1, 10, 100, 1234, etc."
  - help["CLUSTER_TGS"] = "GeuReflector: A comma-separated list of TG numbers that should be broadcast to ALL trunk peers regardless of prefix ownership. These are network-wide channels (like BrandMeister nationwide). Must not overlap with any LOCAL_PREFIX or REMOTE_PREFIX. Example: CLUSTER_TGS=222,2221,91"
  - help["LOCAL_ALIASES"] = "GeuReflector: Re-home a short local TG under one of this reflector's owned prefixes so it routes across the trunk mesh while local users keep dialing the short number. Comma-separated entries of the form localTG:aliasTG[/dir]; aliasTG must fall under a LOCAL_PREFIX. dir is 'in' (mesh->local only), 'out' (local->mesh only), or omitted for two-way. Example: with LOCAL_PREFIX=262, LOCAL_ALIASES=8:2620008 makes local TG 8 reachable mesh-wide as 2620008. Ignored on satellite nodes."
  - help["MQTT_HOST"] = "MQTT broker hostname or IP address. The bundled Mosquitto container is reachable at 'mqtt' from within Docker."
  - help["MQTT_PORT"] = "MQTT broker port. The bundled Mosquitto container listens on 1883. Use 8883 for external brokers with TLS."
  - help["MQTT_USERNAME"] = "Username for MQTT broker authentication."
  - help["MQTT_PASSWORD"] = "Password for MQTT broker authentication."
  - help["MQTT_TOPIC_PREFIX"] = "Base topic path (e.g. svxreflector/myreflector). Must be set to a unique identifier for this reflector. All MQTT messages are published under this prefix."
  - help["MQTT_STATUS_INTERVAL"] = "Periodic full status publish interval in milliseconds. The status topic receives the same JSON as the HTTP /status endpoint. Default: 30000."
  - help["MQTT_TLS_ENABLED"] = "Enable TLS encryption for the MQTT connection. Set to 1 to enable, 0 to disable. Default: 0."
  - help["MQTT_TLS_CA_CERT"] = "Path to the CA certificate file for verifying the broker's TLS certificate. Required when TLS is enabled."
  - help["MQTT_TLS_CLIENT_CERT"] = "Path to the client certificate file for mutual TLS authentication. Optional — only needed if the broker requires client certificates."
  - help["MQTT_TLS_CLIENT_KEY"] = "Path to the client private key file for mutual TLS authentication. Required if TLS_CLIENT_CERT is set."
  - help["MQTT_NAME"] = "Appended as path component to topics: <TOPIC_PREFIX>/<NAME>/... Useful when multiple reflectors share one TOPIC_PREFIX."
  - help["TWIN_HOST"] = "Hostname or IP address of the twin partner reflector."
  - help["TWIN_PORT"] = "TCP port for the twin link. Default: 5304."
  - help["TWIN_SECRET"] = "Shared HMAC secret between twin partners. Must be identical on both sides."
  - help["TWIN_LISTEN_PORT"] = "TCP listen port for inbound twin connections. Default: 5304."
  - help["TRUNK_PAIRED"] = "Set to 1 when the remote peer is a twin pair. Enables multi-host HOST (comma-separated) and dual inbound connections with sticky per-frame failover."
  - help["TRUNK_PEER_ID"] = "Name advertised to the peer in trunk hello. Defaults to the section name."
  - help["TRUNK_BLACKLIST_TGS"] = "Comma-separated TG filter patterns (exact: 666, prefix: 777*, range: 100-199). These TGs are never carried on this link."
  - help["TRUNK_ALLOW_TGS"] = "If set, ONLY these TGs are exchanged. Same syntax as BLACKLIST_TGS."
  - help["TRUNK_TG_MAP"] = "Bidirectional TG remap: peer_tg:local_tg (comma-separated). Mapped TGs bypass prefix routing."
  - help["TRUNK_ROUTABLE_PREFIXES"] = "Comma-separated extra TG prefixes this link will carry, in addition to the peer's REMOTE_PREFIX. Use it to route TGs the peer doesn't own (e.g. cluster or third-party prefixes). Empty = only REMOTE_PREFIX is routed."
  - help["SATELLITE_FILTER"] = "Bidirectional TG scope for this satellite link (same grammar as BLACKLIST_TGS/ALLOW_TGS — exact: 666, prefix: 777*, range: 100-199, comma-separated). Outbound: non-matching local events stay on the satellite. Inbound: the satellite advertises the filter to the parent (MsgTrunkFilter) so the parent skips forwarding non-matching TGs. Empty = no filtering. Requires GeuReflector ≥ v1.3.4 on the parent for inbound suppression."
  - help["REDIS_HOST"] = "Redis server hostname or IP address. Default: 127.0.0.1."
  - help["REDIS_PORT"] = "Redis TCP port. Default: 6379."
  - help["REDIS_PASSWORD"] = "AUTH password for the Redis connection."
  - help["REDIS_DB"] = "Redis logical database index. Default: 0."
  - help["REDIS_KEY_PREFIX"] = "Namespace prefix for all keys (<prefix>:<key>). Required when multiple reflectors share the same Redis instance — use a unique prefix per reflector (e.g. ref1, ref2) to avoid key collisions. Leave empty if this is the only reflector using this Redis."
  - help["REDIS_UNIX_SOCKET"] = "Path to Redis UNIX socket. Takes precedence over HOST/PORT when set."
  - help["REDIS_TLS_ENABLED"] = "Enable TLS for the Redis connection. Set to 1 to enable, 0 to disable. Default: 0."
  - help["REDIS_TLS_CA_CERT"] = "CA certificate file path for verifying the Redis server's TLS certificate."
  - help["REDIS_TLS_CLIENT_CERT"] = "Client certificate file for mutual TLS authentication."
  - help["REDIS_TLS_CLIENT_KEY"] = "Client private key file for mutual TLS authentication."

  %body.text-ghtext
    = render 'shared/navbar', active: 'admin'

    -# Confirm delete modal
    #confirm-modal.fixed.inset-0.z-50.items-center.justify-center.hidden{style: "background: rgba(0,0,0,0.6)"}
      .bg-ghcard.border.border-gray-600.rounded-lg.p-5.max-w-sm.w-full.mx-4.shadow-xl
        .flex.items-center.gap-3.mb-3
          .w-10.h-10.rounded-full.flex.items-center.justify-center.shrink-0{class: "bg-red-500/15"}
            %i.bi.bi-exclamation-triangle.text-red-400
          %h6.text-base.font-semibold.text-gray-100 Remove this item?
        %p.text-sm.text-gray-400.mb-4 This can't be undone. You'll need to re-add it manually.
        .flex.justify-end.gap-2
          %button#confirm-cancel.px-3.py-1.5.rounded-md.border.border-gray-600.text-sm.text-gray-300.hover:text-gray-100.cursor-pointer{type: "button"} Cancel
          %button#confirm-delete.px-3.py-1.5.rounded-md.bg-red-600.hover:bg-red-700.text-sm.text-white.font-semibold.cursor-pointer{type: "button"}
            %i.bi.bi-trash3.mr-1
            Delete

    -# Generic danger confirm modal
    #danger-modal.fixed.inset-0.z-50.items-center.justify-center.hidden{style: "background: rgba(0,0,0,0.6)"}
      .bg-ghcard.border.border-gray-600.rounded-lg.p-5.max-w-sm.w-full.mx-4.shadow-xl
        .flex.items-center.gap-3.mb-3
          .w-10.h-10.rounded-full.flex.items-center.justify-center.shrink-0{class: "bg-red-500/15"}
            %i.bi.bi-exclamation-triangle.text-red-400
          %h6.text-base.font-semibold.text-gray-100#danger-title Action
        %p.text-sm.text-gray-400.mb-4#danger-body Are you sure?
        .flex.justify-end.gap-2
          %button#danger-cancel.px-3.py-1.5.rounded-md.border.border-gray-600.text-sm.text-gray-300.hover:text-gray-100.cursor-pointer{type: "button"} Cancel
          %button#danger-confirm.px-3.py-1.5.rounded-md.bg-red-600.hover:bg-red-700.text-sm.text-white.font-semibold.cursor-pointer{type: "button"}
            %i.bi.bi-trash3.mr-1
            %span#danger-confirm-label Confirm

    -# Backup browser modal
    #backup-modal.fixed.inset-0.z-50.items-center.justify-center.hidden{style: "background: rgba(0,0,0,0.6)"}
      .bg-ghcard.border.border-gray-600.rounded-lg.max-w-3xl.w-full.mx-4.shadow-xl.flex.flex-col{style: "max-height: 85vh"}
        .flex.items-center.justify-between.px-5.py-4.border-b.border-ghborder.shrink-0
          %h6.text-base.font-semibold.text-gray-100
            %i.bi.bi-clock-history.mr-2
            Configuration Backups
          %button.text-gray-400.hover:text-gray-200.cursor-pointer{type: "button", onclick: "closeBackups()"}
            %i.bi.bi-x-lg
        .flex.flex-1.overflow-hidden
          -# Backup list (left panel)
          #backup-list.border-r.border-ghborder.overflow-y-auto.shrink-0{style: "width: 220px"}
            .px-3.py-3.text-sm.text-ghmuted#backup-list-inner
              %span.text-ghdim Loading…
          -# Backup content (right panel)
          #backup-content.flex-1.overflow-y-auto.p-4
            %pre.text-xs.font-mono.text-gray-300.whitespace-pre-wrap#backup-content-pre
              %span.text-ghdim Select a backup to inspect

    -# CSR inspect modal
    #csr-modal.fixed.inset-0.z-50.items-center.justify-center.hidden{style: "background: rgba(0,0,0,0.6)"}
      .bg-ghcard.border.border-gray-600.rounded-lg.max-w-2xl.w-full.mx-4.shadow-xl.flex.flex-col{style: "max-height: 80vh"}
        .flex.items-center.justify-between.px-5.py-4.border-b.border-ghborder.shrink-0
          %h6.text-base.font-semibold.text-gray-100
            %i.bi.bi-file-earmark-lock.mr-2
            %span#csr-modal-title CSR Details
          %button.text-gray-400.hover:text-gray-200.cursor-pointer{type: "button", onclick: "closeCsrModal()"}
            %i.bi.bi-x-lg
        .flex-1.overflow-y-auto.p-5
          %pre.text-xs.font-mono.text-gray-300.whitespace-pre-wrap#csr-modal-body
            %span.text-ghdim Loading…

    -# Help popup modal
    #help-modal.fixed.inset-0.z-50.items-center.justify-center.hidden{style: "background: rgba(0,0,0,0.6)"}
      .bg-ghcard.border.border-gray-600.rounded-lg.p-5.max-w-md.w-full.mx-4.shadow-xl
        .flex.items-center.justify-between.mb-3
          %h6.text-base.font-semibold.text-gray-100#help-modal-title
          %button.text-gray-400.hover:text-gray-200.cursor-pointer{type: "button", onclick: "closeHelp()"}
            %i.bi.bi-x-lg
        %p.text-sm.text-gray-300.leading-relaxed#help-modal-body

    .max-w-3xl.mx-auto.px-4.pb-12
      .flex.items-center.justify-between.mb-6
        %h4.text-xl.font-semibold.text-gray-100
          %i.bi.bi-broadcast.mr-2
          Reflector

      - if flash[:notice]
        %div.p-2.rounded.border.border-green-700.text-green-400.text-sm.mb-4.flex.items-center.justify-between{class: "bg-green-900/30", id: "flash-notice"}
          %span= flash[:notice]
          %button.text-green-400.hover:text-green-200.cursor-pointer{type: "button", onclick: "this.parentElement.remove()"}
            %i.bi.bi-x
      - if flash[:alert]
        %div.p-2.rounded.border.border-red-700.text-red-400.text-sm.mb-4.flex.items-center.justify-between{class: "bg-red-900/30", id: "flash-alert"}
          %span= flash[:alert]
          %button.text-red-400.hover:text-red-200.cursor-pointer{type: "button", onclick: "this.parentElement.remove()"}
            %i.bi.bi-x

      -# ── Tab bar ──
      .flex.border-b.border-gray-600.mb-6
        %button.tab-btn.px-4.py-2.text-sm.font-semibold.border-b-2.cursor-pointer{type: "button", id: "tab-config", onclick: "switchTab('config')", class: "border-blue-500 text-blue-400"}
          %i.bi.bi-gear.mr-1
          Configuration
        %button.tab-btn.px-4.py-2.text-sm.font-semibold.border-b-2.cursor-pointer{type: "button", id: "tab-nodeblock", onclick: "switchTab('nodeblock')", class: "border-transparent text-gray-400 hover:text-gray-200"}
          %i.bi.bi-mic-mute.mr-1
          Node Block
        %button.tab-btn.px-4.py-2.text-sm.font-semibold.border-b-2.cursor-pointer{type: "button", id: "tab-certfiles", onclick: "switchTab('certfiles')", class: "border-transparent text-gray-400 hover:text-gray-200"}
          %i.bi.bi-file-earmark-lock.mr-1
          Certificate Files

      <div id="panel-config">
      = form_with url: admin_reflector_path, method: :patch, local: true, html: { id: "reflector-config-form" } do |f|

        -# ── 1. Global Settings ──
        .rounded-lg.border.border-gray-600.bg-ghcard.mb-4
          .px-4.py-3.border-b.border-gray-600.flex.items-center.justify-between.cursor-pointer{onclick: "toggleSection('global')"}
            %h5.text-lg.font-semibold.text-gray-100
              %i.bi.bi-globe.mr-2
              Global Settings
            %i.bi.bi-chevron-down.accordion-chevron.text-gray-400{id: "chevron-global"}
          .accordion-body.px-4{id: "body-global"}
            %h6.text-sm.font-semibold.text-gray-400.mb-3.mt-1 Core
            .grid.grid-cols-1.md:grid-cols-2.gap-4.mb-4
              - %w[LISTEN_PORT HTTP_SRV_PORT CODECS].each do |key|
                .mb-1
                  %label.block.text-sm.text-gray-500.mb-1
                    = key
                    - if help[key]
                      %button.ml-1.text-gray-500.hover:text-blue-400.cursor-pointer{type: "button", onclick: "showHelp('#{key}')", title: "Help"}
                        %i.bi.bi-question-circle{style: "font-size: 0.75rem"}
                  %input.w-full.px-3.py-2.rounded-md.border.border-gray-600.text-sm{class: "bg-[#0d1117]", type: "text", name: "config[global][#{key}]", value: @config.global[key]}

            %h6.text-sm.font-semibold.text-gray-400.mb-3 Access Control
            .grid.grid-cols-1.md:grid-cols-2.gap-4.mb-4
              - %w[ACCEPT_CALLSIGN REJECT_CALLSIGN ACCEPT_CERT_EMAIL].each do |key|
                .mb-1
                  %label.block.text-sm.text-gray-500.mb-1
                    = key
                    - if help[key]
                      %button.ml-1.text-gray-500.hover:text-blue-400.cursor-pointer{type: "button", onclick: "showHelp('#{key}')", title: "Help"}
                        %i.bi.bi-question-circle{style: "font-size: 0.75rem"}
                  %input.w-full.px-3.py-2.rounded-md.border.border-gray-600.text-sm{class: "bg-[#0d1117]", type: "text", name: "config[global][#{key}]", value: @config.global[key]}

            %h6.text-sm.font-semibold.text-gray-400.mb-3 Timeouts
            .grid.grid-cols-1.md:grid-cols-2.gap-4.mb-4
              - %w[SQL_TIMEOUT SQL_TIMEOUT_BLOCKTIME].each do |key|
                .mb-1
                  %label.block.text-sm.text-gray-500.mb-1
                    = key
                    - if help[key]
                      %button.ml-1.text-gray-500.hover:text-blue-400.cursor-pointer{type: "button", onclick: "showHelp('#{key}')", title: "Help"}
                        %i.bi.bi-question-circle{style: "font-size: 0.75rem"}
                  %input.w-full.px-3.py-2.rounded-md.border.border-gray-600.text-sm{class: "bg-[#0d1117]", type: "text", name: "config[global][#{key}]", value: @config.global[key]}

            %h6.text-sm.font-semibold.text-gray-400.mb-3 Advanced
            .grid.grid-cols-1.md:grid-cols-2.gap-4.mb-4
              - %w[TG_FOR_V1_CLIENTS RANDOM_QSY_RANGE TIMESTAMP_FORMAT LOG COMMAND_PTY CFG_DIR].each do |key|
                .mb-1
                  %label.block.text-sm.text-gray-500.mb-1
                    = key
                    - if help[key]
                      %button.ml-1.text-gray-500.hover:text-blue-400.cursor-pointer{type: "button", onclick: "showHelp('#{key}')", title: "Help"}
                        %i.bi.bi-question-circle{style: "font-size: 0.75rem"}
                  %input.w-full.px-3.py-2.rounded-md.border.border-gray-600.text-sm{class: "bg-[#0d1117]", type: "text", name: "config[global][#{key}]", value: @config.global[key]}

            %h6.text-sm.font-semibold.text-gray-400.mb-3 PKI Paths
            .grid.grid-cols-1.md:grid-cols-2.gap-4
              - %w[CERT_PKI_DIR CERT_CA_KEYS_DIR CERT_CA_PENDING_CSRS_DIR CERT_CA_CSRS_DIR CERT_CA_CERTS_DIR CERT_CA_HOOK].each do |key|
                .mb-1
                  %label.block.text-sm.text-gray-500.mb-1
                    = key
                    - if help[key]
                      %button.ml-1.text-gray-500.hover:text-blue-400.cursor-pointer{type: "button", onclick: "showHelp('#{key}')", title: "Help"}
                        %i.bi.bi-question-circle{style: "font-size: 0.75rem"}
                  %input.w-full.px-3.py-2.rounded-md.border.border-gray-600.text-sm{class: "bg-[#0d1117]", type: "text", name: "config[global][#{key}]", value: @config.global[key]}

        -# ── 1b. Clustering / Trunking (collapsed) ──
        - is_satellite = @config.global['SATELLITE_OF'].present?
        .rounded-lg.border.border-gray-600.bg-ghcard.mb-4
          .px-4.py-3.border-b.border-gray-600.flex.items-center.justify-between.cursor-pointer{onclick: "toggleSection('trunking')"}
            %h5.text-lg.font-semibold.text-gray-100
              %i.bi.bi-diagram-3.mr-2
              Clustering / Trunking
              %span.text-sm.font-normal.text-ghmuted.ml-2 GeuReflector
            .flex.items-center.gap-2
              %button.text-sm.px-3.py-1.rounded-md.border.border-gray-600.text-gray-600.cursor-not-allowed.opacity-50{type: "button", id: "btn-trunking", disabled: true, onclick: "event.stopPropagation(); addTrunkPeer()"}
                %i.bi.bi-plus.mr-1
                Add Trunk Peer
              %i.bi.bi-chevron-down.accordion-chevron.text-gray-400{id: "chevron-trunking"}
          .accordion-body.px-4{id: "body-trunking"}

            -# ── Mode selector ──
            %input{type: "hidden", name: "config[trunk_mode]", value: (is_satellite ? "satellite" : "reflector"), id: "trunk-mode-input"}
            .mb-4
              %label.block.text-sm.font-semibold.text-gray-400.mb-2 This reflector operates as:
              .inline-flex.rounded-lg.border.border-gray-600.overflow-hidden
                %button.px-4.py-2.text-sm.font-semibold.cursor-pointer.transition-colors{type: "button", id: "mode-btn-reflector", class: (is_satellite ? "text-gray-400 hover:text-gray-200" : "bg-blue-600 text-white"), onclick: "setTrunkMode('reflector')"}
                  %i.bi.bi-broadcast-pin.mr-1
                  Reflector
                %button.px-4.py-2.text-sm.font-semibold.cursor-pointer.transition-colors.border-l.border-gray-600{type: "button", id: "mode-btn-satellite", class: (is_satellite ? "bg-purple-600 text-white" : "text-gray-400 hover:text-gray-200"), onclick: "setTrunkMode('satellite')"}
                  %i.bi.bi-broadcast.mr-1
                  Satellite

            -# ════════════════════════════════════════════════════════════════
            -# REFLECTOR MODE sections
            -# ════════════════════════════════════════════════════════════════
            #trunk-mode-reflector{class: (is_satellite ? 'hidden' : nil)}
              .p-2.rounded.border.text-xs.mb-4{class: "border-blue-700/50 text-blue-400 bg-blue-900/20"}
                %i.bi.bi-info-circle.mr-1
                Reflector mode: this instance owns talkgroups and can trunk with other reflectors. Configure your local TG prefix, cluster TGs, trunk peers, and optionally accept satellite connections.

              %h6.text-sm.font-semibold.text-gray-400.mb-3 TG Ownership
              .grid.grid-cols-1.md:grid-cols-2.gap-4.mb-4
                .mb-1
                  %label.block.text-sm.text-gray-500.mb-1
                    LOCAL_PREFIX
                    %button.ml-1.text-gray-500.hover:text-blue-400.cursor-pointer{type: "button", onclick: "showHelp('LOCAL_PREFIX')", title: "Help"}
                      %i.bi.bi-question-circle{style: "font-size: 0.75rem"}
                  %input.w-full.px-3.py-2.rounded-md.border.border-gray-600.text-sm{class: "bg-[#0d1117]", type: "text", name: "config[global][LOCAL_PREFIX]", value: @config.global['LOCAL_PREFIX'], placeholder: "e.g. 1,24"}
                .mb-1
                  %label.block.text-sm.text-gray-500.mb-1
                    CLUSTER_TGS
                    %button.ml-1.text-gray-500.hover:text-blue-400.cursor-pointer{type: "button", onclick: "showHelp('CLUSTER_TGS')", title: "Help"}
                      %i.bi.bi-question-circle{style: "font-size: 0.75rem"}
                  %input.w-full.px-3.py-2.rounded-md.border.border-gray-600.text-sm{class: "bg-[#0d1117]", type: "text", name: "config[global][CLUSTER_TGS]", value: @config.global['CLUSTER_TGS'], placeholder: "e.g. 222,2221,91"}
              .mb-4
                %label.block.text-sm.text-gray-500.mb-1
                  LOCAL_ALIASES
                  %button.ml-1.text-gray-500.hover:text-blue-400.cursor-pointer{type: "button", onclick: "showHelp('LOCAL_ALIASES')", title: "Help"}
                    %i.bi.bi-question-circle{style: "font-size: 0.75rem"}
                %input.w-full.px-3.py-2.rounded-md.border.border-gray-600.text-sm{class: "bg-[#0d1117]", type: "text", name: "config[global][LOCAL_ALIASES]", value: @config.global['LOCAL_ALIASES'], placeholder: "e.g. 8:2620008,9:2620009/out"}

              %h6.text-sm.font-semibold.text-gray-400.mb-3 Trunk Peers
              #trunk-peers.space-y-4
                - @config.trunks.keys.sort.each do |trunk_name|
                  - trunk = @config.trunks[trunk_name]
                  .trunk-peer.border.border-gray-700.rounded-lg.p-4.relative
                    %button.absolute.top-2.right-2.text-red-400.hover:text-red-300.cursor-pointer{type: "button", onclick: "confirmRemove(this, '.trunk-peer')"}
                      %i.bi.bi-x-lg
                    .grid.grid-cols-1.md:grid-cols-2.gap-3
                      .col-span-1
                        %label.block.text-sm.text-gray-500.mb-1
                          Section Name
                          %button.ml-1.text-gray-500.hover:text-blue-400.cursor-pointer{type: "button", onclick: "showHelp('TRUNK_SECTION_NAME')", title: "Help"}
                            %i.bi.bi-question-circle{style: "font-size: 0.75rem"}
                        .flex.items-center.gap-0
                          %span.px-2.py-1.rounded-l.border.border-r-0.border-gray-600.text-sm.text-gray-500{class: "bg-[#161b22]"} TRUNK_
                          %input.w-full.px-2.py-1.rounded-r.border.border-gray-600.text-sm.uppercase{class: "bg-[#0d1117]", type: "text", name: "config[trunk_names][]", value: trunk_name.delete_prefix("TRUNK_"), placeholder: "REFA_REFB"}
                      .col-span-1
                        %label.block.text-sm.text-gray-500.mb-1 HOST
                        %input.w-full.px-2.py-1.rounded.border.border-gray-600.text-sm{class: "bg-[#0d1117]", type: "text", name: "config[trunk_hosts][]", value: trunk['HOST']}
                      .col-span-1
                        %label.block.text-sm.text-gray-500.mb-1 PORT
                        %input.w-full.px-2.py-1.rounded.border.border-gray-600.text-sm{class: "bg-[#0d1117]", type: "text", name: "config[trunk_ports][]", value: (trunk['PORT'] || '5302')}
                      .col-span-1
                        %label.block.text-sm.text-gray-500.mb-1 SECRET
                        %input.w-full.px-2.py-1.rounded.border.border-gray-600.text-sm{class: "bg-[#0d1117]", type: "text", name: "config[trunk_secrets][]", value: trunk['SECRET']}
                      .col-span-2
                        %label.block.text-sm.text-gray-500.mb-1 REMOTE_PREFIX (comma-separated)
                        %input.w-full.px-2.py-1.rounded.border.border-gray-600.text-sm{class: "bg-[#0d1117]", type: "text", name: "config[trunk_remote_prefixes][]", value: trunk['REMOTE_PREFIX'], placeholder: "2,3"}
                      .col-span-2
                        %label.block.text-sm.text-gray-500.mb-1
                          Remote status URL
                        %input.w-full.px-2.py-1.rounded.border.border-gray-600.text-sm{class: "bg-[#0d1117]", type: "url", name: "config[trunk_status_urls][]", value: trunk['STATUS_URL'], placeholder: "http://reflector-b.example.com:8080/status"}
                      .col-span-2
                        %details.text-xs
                          %summary.text-gray-500.cursor-pointer.hover:text-gray-300 Advanced trunk options
                          .grid.grid-cols-1.md:grid-cols-2.gap-3.mt-3
                            .col-span-1
                              %label.block.text-sm.text-gray-500.mb-1
                                PEER_ID
                                %button.ml-1.text-gray-500.hover:text-blue-400.cursor-pointer{type: "button", onclick: "showHelp('TRUNK_PEER_ID')", title: "Help"}
                                  %i.bi.bi-question-circle{style: "font-size: 0.75rem"}
                              %input.w-full.px-2.py-1.rounded.border.border-gray-600.text-sm{class: "bg-[#0d1117]", type: "text", name: "config[trunk_peer_ids][]", value: trunk['PEER_ID']}
                            .col-span-1
                              %label.block.text-sm.text-gray-500.mb-1
                                PAIRED (twin peer)
                                %button.ml-1.text-gray-500.hover:text-blue-400.cursor-pointer{type: "button", onclick: "showHelp('TRUNK_PAIRED')", title: "Help"}
                                  %i.bi.bi-question-circle{style: "font-size: 0.75rem"}
                              %select.w-full.px-2.py-1.rounded.border.border-gray-600.text-sm{class: "bg-[#0d1117]", name: "config[trunk_paireds][]"}
                                %option{value: "0", selected: trunk['PAIRED'] != '1'} No
                                %option{value: "1", selected: trunk['PAIRED'] == '1'} Yes
                            .col-span-1
                              %label.block.text-sm.text-gray-500.mb-1
                                BLACKLIST_TGS
                                %button.ml-1.text-gray-500.hover:text-blue-400.cursor-pointer{type: "button", onclick: "showHelp('TRUNK_BLACKLIST_TGS')", title: "Help"}
                                  %i.bi.bi-question-circle{style: "font-size: 0.75rem"}
                              %input.w-full.px-2.py-1.rounded.border.border-gray-600.text-sm{class: "bg-[#0d1117]", type: "text", name: "config[trunk_blacklist_tgs][]", value: trunk['BLACKLIST_TGS'], placeholder: "666,777*,100-199"}
                            .col-span-1
                              %label.block.text-sm.text-gray-500.mb-1
                                ALLOW_TGS
                                %button.ml-1.text-gray-500.hover:text-blue-400.cursor-pointer{type: "button", onclick: "showHelp('TRUNK_ALLOW_TGS')", title: "Help"}
                                  %i.bi.bi-question-circle{style: "font-size: 0.75rem"}
                              %input.w-full.px-2.py-1.rounded.border.border-gray-600.text-sm{class: "bg-[#0d1117]", type: "text", name: "config[trunk_allow_tgs][]", value: trunk['ALLOW_TGS']}
                            .col-span-2
                              %label.block.text-sm.text-gray-500.mb-1
                                TG_MAP
                                %button.ml-1.text-gray-500.hover:text-blue-400.cursor-pointer{type: "button", onclick: "showHelp('TRUNK_TG_MAP')", title: "Help"}
                                  %i.bi.bi-question-circle{style: "font-size: 0.75rem"}
                              %input.w-full.px-2.py-1.rounded.border.border-gray-600.text-sm{class: "bg-[#0d1117]", type: "text", name: "config[trunk_tg_maps][]", value: trunk['TG_MAP'], placeholder: "100:200,101:201"}
                            .col-span-2
                              %label.block.text-sm.text-gray-500.mb-1
                                ROUTABLE_PREFIXES (comma-separated)
                                %button.ml-1.text-gray-500.hover:text-blue-400.cursor-pointer{type: "button", onclick: "showHelp('TRUNK_ROUTABLE_PREFIXES')", title: "Help"}
                                  %i.bi.bi-question-circle{style: "font-size: 0.75rem"}
                              %input.w-full.px-2.py-1.rounded.border.border-gray-600.text-sm{class: "bg-[#0d1117]", type: "text", name: "config[trunk_routable_prefixes][]", value: trunk['ROUTABLE_PREFIXES'], placeholder: "9,72"}

              %h6.text-sm.font-semibold.text-gray-400.mb-3.mt-4 Twin (HA Pair)
              .p-2.rounded.border.text-xs.mb-3{class: "border-gray-700 text-ghmuted bg-ghbg"}
                %i.bi.bi-info-circle.mr-1
                Optional. Pair this reflector with a twin partner for high-availability. Both twins share the same LOCAL_PREFIX and mirror all state symmetrically.
              .grid.grid-cols-1.md:grid-cols-3.gap-4.mb-4
                .mb-1
                  %label.block.text-sm.text-gray-500.mb-1
                    HOST
                    %button.ml-1.text-gray-500.hover:text-blue-400.cursor-pointer{type: "button", onclick: "showHelp('TWIN_HOST')", title: "Help"}
                      %i.bi.bi-question-circle{style: "font-size: 0.75rem"}
                  %input.w-full.px-3.py-2.rounded-md.border.border-gray-600.text-sm{class: "bg-[#0d1117]", type: "text", name: "config[twin][HOST]", value: @config.twin['HOST'], placeholder: "twin-partner.example.com"}
                .mb-1
                  %label.block.text-sm.text-gray-500.mb-1
                    PORT
                    %button.ml-1.text-gray-500.hover:text-blue-400.cursor-pointer{type: "button", onclick: "showHelp('TWIN_PORT')", title: "Help"}
                      %i.bi.bi-question-circle{style: "font-size: 0.75rem"}
                  %input.w-full.px-3.py-2.rounded-md.border.border-gray-600.text-sm{class: "bg-[#0d1117]", type: "text", name: "config[twin][PORT]", value: @config.twin['PORT'], placeholder: "5304"}
                .mb-1
                  %label.block.text-sm.text-gray-500.mb-1
                    SECRET
                    %button.ml-1.text-gray-500.hover:text-blue-400.cursor-pointer{type: "button", onclick: "showHelp('TWIN_SECRET')", title: "Help"}
                      %i.bi.bi-question-circle{style: "font-size: 0.75rem"}
                  %input.w-full.px-3.py-2.rounded-md.border.border-gray-600.text-sm{class: "bg-[#0d1117]", type: "text", name: "config[twin][SECRET]", value: @config.twin['SECRET']}
              .grid.grid-cols-1.md:grid-cols-2.gap-4.mb-4
                .mb-1
                  %label.block.text-sm.text-gray-500.mb-1
                    TWIN_LISTEN_PORT
                    %button.ml-1.text-gray-500.hover:text-blue-400.cursor-pointer{type: "button", onclick: "showHelp('TWIN_LISTEN_PORT')", title: "Help"}
                      %i.bi.bi-question-circle{style: "font-size: 0.75rem"}
                  %input.w-full.px-3.py-2.rounded-md.border.border-gray-600.text-sm{class: "bg-[#0d1117]", type: "text", name: "config[global][TWIN_LISTEN_PORT]", value: @config.global['TWIN_LISTEN_PORT'], placeholder: "5304"}

              %h6.text-sm.font-semibold.text-gray-400.mb-3.mt-4 Accept Satellite Connections
              .p-2.rounded.border.text-xs.mb-3{class: "border-gray-700 text-ghmuted bg-ghbg"}
                %i.bi.bi-info-circle.mr-1
                Optional. Configure a listener so satellite instances can connect to this reflector.
              .grid.grid-cols-1.md:grid-cols-2.gap-4.mb-4
                .mb-1
                  %label.block.text-sm.text-gray-500.mb-1 LISTEN_PORT
                  %input.w-full.px-3.py-2.rounded-md.border.border-gray-600.text-sm{class: "bg-[#0d1117]", type: "text", name: "config[satellite][LISTEN_PORT]", value: @config.satellite['LISTEN_PORT'], placeholder: "5303"}
                .mb-1
                  %label.block.text-sm.text-gray-500.mb-1 SECRET (fallback)
                  %input.w-full.px-3.py-2.rounded-md.border.border-gray-600.text-sm{class: "bg-[#0d1117]", type: "text", name: "config[satellite][SECRET]", value: @config.satellite['SECRET']}
                  %small.text-gray-500.block.mt-1
                    Used when no per-id secret matches the connecting satellite. Optional if per-id secrets are configured below.

              %h6.text-sm.font-semibold.text-gray-400.mb-3.mt-4 Per-satellite secrets
              .p-2.rounded.border.text-xs.mb-3{class: "border-gray-700 text-ghmuted bg-ghbg"}
                %i.bi.bi-info-circle.mr-1
                Match by the satellite's SATELLITE_ID. Per-id secrets take precedence over the fallback; if a satellite's id has no entry here, it uses the fallback SECRET (or is rejected if no fallback is set). Allowed id characters: A-Z, a-z, 0-9, dash.
              #satellite-secret-rows
                - @config.satellite_per_id_secrets.each do |id, secret|
                  .satellite-secret-row.grid.grid-cols-12.gap-2.mb-2
                    .col-span-4
                      %input.w-full.px-3.py-2.rounded-md.border.border-gray-600.text-sm{class: "bg-[#0d1117]", type: "text", name: "config[satellite_secret_ids][]", value: id, pattern: "[A-Za-z0-9\\-]+", placeholder: "satellite-id"}
                    .col-span-7
                      %input.w-full.px-3.py-2.rounded-md.border.border-gray-600.text-sm{class: "bg-[#0d1117]", type: "text", name: "config[satellite_secret_values][]", value: secret, placeholder: "secret"}
                    .col-span-1.flex.items-center
                      %button.text-red-400.hover:text-red-300.cursor-pointer{type: "button", onclick: "removeSatelliteSecret(this, #{id.to_json})", title: "Remove"}
                        %i.bi.bi-x-lg
              %button.mt-2.px-3.py-1.text-xs.rounded.border.border-gray-600.text-gray-300.hover:bg-gray-700.cursor-pointer{type: "button", onclick: "addSatelliteSecret()"}
                %i.bi.bi-plus-lg.mr-1
                Add satellite secret

            -# ════════════════════════════════════════════════════════════════
            -# SATELLITE MODE sections
            -# ════════════════════════════════════════════════════════════════
            #trunk-mode-satellite{class: (is_satellite ? nil : 'hidden')}
              .p-2.rounded.border.text-xs.mb-4{class: "border-purple-700/50 text-purple-300 bg-purple-900/20"}
                %i.bi.bi-info-circle.mr-1
                Satellite mode: this instance connects to a parent reflector and relays traffic. It does not own talkgroups, manage trunk peers, or accept satellite connections. Only configure the parent connection below.

              %h6.text-sm.font-semibold.text-gray-400.mb-3 Parent Reflector Connection
              .grid.grid-cols-1.md:grid-cols-2.gap-4.mb-4
                .mb-1
                  %label.block.text-sm.text-gray-500.mb-1 SATELLITE_OF (parent host)
                  %input.w-full.px-3.py-2.rounded-md.border.border-gray-600.text-sm{class: "bg-[#0d1117]", type: "text", name: "config[global][SATELLITE_OF]", value: @config.global['SATELLITE_OF'], placeholder: "reflector-01.example.com", id: "satellite-of-input"}
                .mb-1
                  %label.block.text-sm.text-gray-500.mb-1 SATELLITE_PORT
                  %input.w-full.px-3.py-2.rounded-md.border.border-gray-600.text-sm{class: "bg-[#0d1117]", type: "text", name: "config[global][SATELLITE_PORT]", value: (@config.global['SATELLITE_PORT'] || '5303')}
                .mb-1
                  %label.block.text-sm.text-gray-500.mb-1 SATELLITE_SECRET
                  %input.w-full.px-3.py-2.rounded-md.border.border-gray-600.text-sm{class: "bg-[#0d1117]", type: "text", name: "config[global][SATELLITE_SECRET]", value: @config.global['SATELLITE_SECRET']}
                .mb-1
                  %label.block.text-sm.text-gray-500.mb-1 SATELLITE_ID
                  %input.w-full.px-3.py-2.rounded-md.border.border-gray-600.text-sm{class: "bg-[#0d1117]", type: "text", name: "config[global][SATELLITE_ID]", value: @config.global['SATELLITE_ID']}
              .grid.grid-cols-1.gap-4.mb-4
                .mb-1
                  %label.block.text-sm.text-gray-500.mb-1
                    SATELLITE_FILTER
                    %button.ml-1.text-gray-500.hover:text-blue-400.cursor-pointer{type: "button", onclick: "showHelp('SATELLITE_FILTER')", title: "Help"}
                      %i.bi.bi-question-circle{style: "font-size:0.75rem"}
                  %input.w-full.px-3.py-2.rounded-md.border.border-gray-600.text-sm{class: "bg-[#0d1117]", type: "text", name: "config[global][SATELLITE_FILTER]", value: @config.global['SATELLITE_FILTER'], placeholder: "24*,262*,2427-2438"}
                  %small.text-gray-500.block.mt-1 Bidirectional TG scope. Leave empty to carry all TGs. Requires GeuReflector ≥ v1.3.4 on the parent for inbound suppression.
                .mb-1
                  %label.block.text-sm.text-gray-500.mb-1
                    Parent status URL
                    %span.text-xs.text-ghdim.ml-1 (dashboard only)
                  %input.w-full.px-3.py-2.rounded-md.border.border-gray-600.text-sm{class: "bg-[#0d1117]", type: "url", name: "config[global][SATELLITE_STATUS_URL]", value: @config.global['SATELLITE_STATUS_URL'], placeholder: "https://parent-reflector.example.com/status", id: "satellite-status-url-input"}
                  %small.text-gray-500.block.mt-1 Polled by the dashboard to show parent node count and status on /trunks. Not sent to the reflector.

        -# ── 2. Certificates (collapsed) ──
        .rounded-lg.border.border-gray-600.bg-ghcard.mb-4
          .px-4.py-3.border-b.border-gray-600.flex.items-center.justify-between.cursor-pointer{onclick: "toggleSection('certs')"}
            %h5.text-lg.font-semibold.text-gray-100
              %i.bi.bi-shield-lock.mr-2
              Certificates
              - if @has_certs
                %i.bi.bi-lock-fill.text-yellow-400.text-sm.ml-2
            %i.bi.bi-chevron-down.accordion-chevron.text-gray-400{id: "chevron-certs"}
          .accordion-body.px-4{id: "body-certs"}
            - if @has_certs
              .p-3.rounded.border.text-sm.mb-4.flex.items-center.gap-2{class: "border-yellow-700/50 text-yellow-400 bg-yellow-900/20"}
                %i.bi.bi-lock-fill
                Certificates are already generated. To modify these fields, reset PKI from the
                %strong Certificate Files
                tab first.
            - cert_fields = %w[COMMON_NAME ORG_UNIT ORG LOCALITY STATE COUNTRY EMAIL_ADDRESS SUBJECT_ALT_NAME]
            - [["ROOT_CA", :root_ca, @config.root_ca], ["ISSUING_CA", :issuing_ca, @config.issuing_ca], ["SERVER_CERT", :server_cert, @config.server_cert]].each do |label, param_key, data|
              %h6.text-sm.font-semibold.text-gray-400.mb-3.mt-4= label
              .grid.grid-cols-1.md:grid-cols-2.gap-4.mb-2
                - cert_fields.each do |key|
                  .mb-1
                    %label.block.text-sm.text-gray-500.mb-1
                      = key
                      - if help[key]
                        %button.ml-1.text-gray-500.hover:text-blue-400.cursor-pointer{type: "button", onclick: "showHelp('#{key}')", title: "Help"}
                          %i.bi.bi-question-circle{style: "font-size: 0.75rem"}
                    %input.w-full.px-3.py-2.rounded-md.border.border-gray-600.text-sm{class: "bg-[#0d1117]", type: "text", name: "config[#{param_key}][#{key}]", value: data[key], disabled: @has_certs}

        -# ── 3. Users (collapsed) ──
        .rounded-lg.border.border-gray-600.bg-ghcard.mb-4
          .px-4.py-3.border-b.border-gray-600.flex.items-center.justify-between.cursor-pointer{onclick: "toggleSection('users')"}
            %h5.text-lg.font-semibold.text-gray-100
              %i.bi.bi-people.mr-2
              Users
              - if @config.redis_mode?
                %span.text-xs.font-normal.ml-2.px-1.5.py-0.5.rounded{class: "bg-red-900/40 text-red-400 border border-red-700/50"}
                  %i.bi.bi-database.mr-1{style: "font-size: 0.65rem"}
                  Redis
            .flex.items-center.gap-2
              %button.text-sm.px-3.py-1.rounded-md.border.border-gray-600.text-gray-600.cursor-not-allowed.opacity-50{type: "button", id: "btn-users", disabled: true, onclick: "event.stopPropagation(); addUserRow()"}
                %i.bi.bi-plus.mr-1
                Add
              %i.bi.bi-chevron-down.accordion-chevron.text-gray-400{id: "chevron-users"}
          .accordion-body.px-4{id: "body-users"}
            %table.w-full.text-sm
              %thead
                %tr.text-gray-500.text-left
                  %th.pb-2.pr-2 Callsign
                  %th.pb-2.pr-2 Password Group
                  %th.pb-2.w-10
              %tbody#users-tbody
                - @config.users.each do |callsign, group|
                  %tr.border-t.border-gray-700
                    %td.py-2.pr-2
                      %input.w-full.px-2.py-1.rounded.border.border-gray-600.text-sm{class: "bg-[#0d1117]", type: "text", name: "config[user_callsigns][]", value: callsign}
                    %td.py-2.pr-2
                      %select.user-group-select.w-full.px-2.py-1.rounded.border.border-gray-600.text-sm{class: "bg-[#0d1117]", name: "config[user_groups][]", data: { selected: group }}
                    %td.py-2.text-center
                      %button.text-red-400.hover:text-red-300.cursor-pointer{type: "button", onclick: "confirmRemove(this, 'tr')"}
                        %i.bi.bi-x-lg

        -# ── 4. Passwords (collapsed) ──
        .rounded-lg.border.border-gray-600.bg-ghcard.mb-4
          .px-4.py-3.border-b.border-gray-600.flex.items-center.justify-between.cursor-pointer{onclick: "toggleSection('passwords')"}
            %h5.text-lg.font-semibold.text-gray-100
              %i.bi.bi-key.mr-2
              Passwords
              - if @config.redis_mode?
                %span.text-xs.font-normal.ml-2.px-1.5.py-0.5.rounded{class: "bg-red-900/40 text-red-400 border border-red-700/50"}
                  %i.bi.bi-database.mr-1{style: "font-size: 0.65rem"}
                  Redis
            .flex.items-center.gap-2
              %button.text-sm.px-3.py-1.rounded-md.border.border-gray-600.text-gray-600.cursor-not-allowed.opacity-50{type: "button", id: "btn-passwords", disabled: true, onclick: "event.stopPropagation(); addPasswordRow()"}
                %i.bi.bi-plus.mr-1
                Add
              %i.bi.bi-chevron-down.accordion-chevron.text-gray-400{id: "chevron-passwords"}
          .accordion-body.px-4{id: "body-passwords"}
            %table.w-full.text-sm
              %thead
                %tr.text-gray-500.text-left
                  %th.pb-2.pr-2 Group Name
                  %th.pb-2.pr-2 Password
                  %th.pb-2.w-10
              %tbody#passwords-tbody
                - @config.passwords.each do |name, password|
                  %tr.border-t.border-gray-700
                    %td.py-2.pr-2
                      %input.w-full.px-2.py-1.rounded.border.border-gray-600.text-sm{class: "bg-[#0d1117]", type: "text", name: "config[password_names][]", value: name}
                    %td.py-2.pr-2
                      %input.w-full.px-2.py-1.rounded.border.border-gray-600.text-sm{class: "bg-[#0d1117]", type: "text", name: "config[password_values][]", value: password}
                    %td.py-2.text-center
                      %button.text-red-400.hover:text-red-300.cursor-pointer{type: "button", onclick: "confirmRemove(this, 'tr')"}
                        %i.bi.bi-x-lg

        -# ── 5. Talkgroup Rules (collapsed) ──
        .rounded-lg.border.border-gray-600.bg-ghcard.mb-4
          .px-4.py-3.border-b.border-gray-600.flex.items-center.justify-between.cursor-pointer{onclick: "toggleSection('tg')"}
            %h5.text-lg.font-semibold.text-gray-100
              %i.bi.bi-collection.mr-2
              Talkgroup Rules
            .flex.items-center.gap-2
              %button.text-sm.px-3.py-1.rounded-md.border.border-gray-600.text-gray-600.cursor-not-allowed.opacity-50{type: "button", id: "btn-tg", disabled: true, onclick: "event.stopPropagation(); addTgRule()"}
                %i.bi.bi-plus.mr-1
                Add TG Rule
              %i.bi.bi-chevron-down.accordion-chevron.text-gray-400{id: "chevron-tg"}
          .accordion-body.px-4{id: "body-tg"}
            #tg-rules.space-y-4
              - @config.tg_rules.keys.sort.each do |tg_num|
                - rules = @config.tg_rules[tg_num]
                .tg-rule.border.border-gray-700.rounded-lg.p-4.relative
                  %button.absolute.top-2.right-2.text-red-400.hover:text-red-300.cursor-pointer{type: "button", onclick: "confirmRemove(this, '.tg-rule')"}
                    %i.bi.bi-x-lg
                  .grid.grid-cols-1.md:grid-cols-2.gap-3
                    .col-span-1
                      %label.block.text-sm.text-gray-500.mb-1 TG Number
                      %input.w-full.px-2.py-1.rounded.border.border-gray-600.text-sm{class: "bg-[#0d1117]", type: "number", name: "config[tg_numbers][]", value: tg_num, min: 1}
                    .col-span-1
                      %label.block.text-sm.text-gray-500.mb-1
                        ALLOW (regex)
                        %button.ml-1.text-gray-500.hover:text-blue-400.cursor-pointer{type: "button", onclick: "showHelp('ALLOW')", title: "Help"}
                          %i.bi.bi-question-circle{style: "font-size: 0.75rem"}
                      %input.w-full.px-2.py-1.rounded.border.border-gray-600.text-sm{class: "bg-[#0d1117]", type: "text", name: "config[tg_allows][]", value: rules["ALLOW"], placeholder: "^.*$"}
                    .col-span-1
                      %label.block.text-sm.text-gray-500.mb-1
                        AUTO_QSY_AFTER (seconds)
                        %button.ml-1.text-gray-500.hover:text-blue-400.cursor-pointer{type: "button", onclick: "showHelp('AUTO_QSY_AFTER')", title: "Help"}
                          %i.bi.bi-question-circle{style: "font-size: 0.75rem"}
                      %input.w-full.px-2.py-1.rounded.border.border-gray-600.text-sm{class: "bg-[#0d1117]", type: "number", name: "config[tg_auto_qsys][]", value: rules["AUTO_QSY_AFTER"]}
                    .col-span-1
                      %label.block.text-sm.text-gray-500.mb-1
                        SHOW_ACTIVITY
                        %button.ml-1.text-gray-500.hover:text-blue-400.cursor-pointer{type: "button", onclick: "showHelp('SHOW_ACTIVITY')", title: "Help"}
                          %i.bi.bi-question-circle{style: "font-size: 0.75rem"}
                      %select.w-full.px-2.py-1.rounded.border.border-gray-600.text-sm{class: "bg-[#0d1117]", name: "config[tg_show_activities][]"}
                        %option{value: "", selected: rules["SHOW_ACTIVITY"].blank?} (not set)
                        %option{value: "1", selected: rules["SHOW_ACTIVITY"] == "1"} Yes
                        %option{value: "0", selected: rules["SHOW_ACTIVITY"] == "0"} No
                    .col-span-1
                      %label.block.text-sm.text-gray-500.mb-1
                        ALLOW_MONITOR (regex)
                        %button.ml-1.text-gray-500.hover:text-blue-400.cursor-pointer{type: "button", onclick: "showHelp('ALLOW_MONITOR')", title: "Help"}
                          %i.bi.bi-question-circle{style: "font-size: 0.75rem"}
                      %input.w-full.px-2.py-1.rounded.border.border-gray-600.text-sm{class: "bg-[#0d1117]", type: "text", name: "config[tg_allow_monitors][]", value: rules["ALLOW_MONITOR"], placeholder: "^.*$"}

        -# ── 6. MQTT (collapsed) ──
        .rounded-lg.border.border-gray-600.bg-ghcard.mb-4
          .px-4.py-3.border-b.border-gray-600.flex.items-center.justify-between.cursor-pointer{onclick: "toggleSection('mqtt')"}
            %h5.text-lg.font-semibold.text-gray-100
              %i.bi.bi-cloud-arrow-up.mr-2
              MQTT
              %span.text-sm.font-normal.text-ghmuted.ml-2 GeuReflector
            %i.bi.bi-chevron-down.accordion-chevron.text-gray-400{id: "chevron-mqtt"}
          .accordion-body.px-4{id: "body-mqtt"}
            .p-2.rounded.border.text-xs.mb-4{class: "border-blue-700/50 text-blue-400 bg-blue-900/20"}
              %i.bi.bi-info-circle.mr-1
              Publish reflector events (talker, client, trunk state) and periodic status to an MQTT broker. Leave the section empty to disable MQTT (zero overhead).

            %h6.text-sm.font-semibold.text-gray-400.mb-3 Connection
            .grid.grid-cols-1.md:grid-cols-2.gap-4.mb-4
              - { "HOST" => "mqtt.example.com", "PORT" => "1883", "USERNAME" => nil, "PASSWORD" => nil }.each do |key, ph|
                .mb-1
                  %label.block.text-sm.text-gray-500.mb-1
                    = key
                    - if help["MQTT_#{key}"]
                      %button.ml-1.text-gray-500.hover:text-blue-400.cursor-pointer{type: "button", onclick: "showHelp('MQTT_#{key}')", title: "Help"}
                        %i.bi.bi-question-circle{style: "font-size: 0.75rem"}
                  %input.w-full.px-3.py-2.rounded-md.border.border-gray-600.text-sm{class: "bg-[#0d1117]", type: "text", name: "config[mqtt][#{key}]", value: @config.mqtt[key], placeholder: ph}

            %h6.text-sm.font-semibold.text-gray-400.mb-3 Topics
            .grid.grid-cols-1.md:grid-cols-3.gap-4.mb-4
              .mb-1
                %label.block.text-sm.text-gray-500.mb-1
                  TOPIC_PREFIX
                  %button.ml-1.text-gray-500.hover:text-blue-400.cursor-pointer{type: "button", onclick: "showHelp('MQTT_TOPIC_PREFIX')", title: "Help"}
                    %i.bi.bi-question-circle{style: "font-size: 0.75rem"}
                %input.w-full.px-3.py-2.rounded-md.border.border-gray-600.text-sm{class: "bg-[#0d1117]", type: "text", name: "config[mqtt][TOPIC_PREFIX]", value: @config.mqtt['TOPIC_PREFIX'], placeholder: "svxreflector/myreflector"}
              .mb-1
                %label.block.text-sm.text-gray-500.mb-1
                  MQTT_NAME
                  %button.ml-1.text-gray-500.hover:text-blue-400.cursor-pointer{type: "button", onclick: "showHelp('MQTT_NAME')", title: "Help"}
                    %i.bi.bi-question-circle{style: "font-size: 0.75rem"}
                %input.w-full.px-3.py-2.rounded-md.border.border-gray-600.text-sm{class: "bg-[#0d1117]", type: "text", name: "config[mqtt][MQTT_NAME]", value: @config.mqtt['MQTT_NAME']}
              .mb-1
                %label.block.text-sm.text-gray-500.mb-1
                  STATUS_INTERVAL (ms)
                  %button.ml-1.text-gray-500.hover:text-blue-400.cursor-pointer{type: "button", onclick: "showHelp('MQTT_STATUS_INTERVAL')", title: "Help"}
                    %i.bi.bi-question-circle{style: "font-size: 0.75rem"}
                %input.w-full.px-3.py-2.rounded-md.border.border-gray-600.text-sm{class: "bg-[#0d1117]", type: "number", name: "config[mqtt][STATUS_INTERVAL]", value: @config.mqtt['STATUS_INTERVAL'], placeholder: "30000"}

            %h6.text-sm.font-semibold.text-gray-400.mb-3 TLS
            .grid.grid-cols-1.md:grid-cols-2.gap-4
              .mb-1
                %label.block.text-sm.text-gray-500.mb-1
                  TLS_ENABLED
                  %button.ml-1.text-gray-500.hover:text-blue-400.cursor-pointer{type: "button", onclick: "showHelp('MQTT_TLS_ENABLED')", title: "Help"}
                    %i.bi.bi-question-circle{style: "font-size: 0.75rem"}
                %select.w-full.px-3.py-2.rounded-md.border.border-gray-600.text-sm{class: "bg-[#0d1117]", name: "config[mqtt][TLS_ENABLED]", id: "mqtt-tls-enabled", onchange: "toggleMqttTls()"}
                  %option{value: "", selected: @config.mqtt['TLS_ENABLED'].blank?} (not set)
                  %option{value: "0", selected: @config.mqtt['TLS_ENABLED'] == '0'} Disabled
                  %option{value: "1", selected: @config.mqtt['TLS_ENABLED'] == '1'} Enabled
              .mb-1
                %label.block.text-sm.text-gray-500.mb-1
                  TLS_CA_CERT
                  %button.ml-1.text-gray-500.hover:text-blue-400.cursor-pointer{type: "button", onclick: "showHelp('MQTT_TLS_CA_CERT')", title: "Help"}
                    %i.bi.bi-question-circle{style: "font-size: 0.75rem"}
                %input.w-full.px-3.py-2.rounded-md.border.border-gray-600.text-sm.mqtt-tls-field{class: "bg-[#0d1117]", type: "text", name: "config[mqtt][TLS_CA_CERT]", value: @config.mqtt['TLS_CA_CERT'], placeholder: "/etc/svxlink/mqtt-ca.crt"}
              .mb-1
                %label.block.text-sm.text-gray-500.mb-1
                  TLS_CLIENT_CERT
                  %button.ml-1.text-gray-500.hover:text-blue-400.cursor-pointer{type: "button", onclick: "showHelp('MQTT_TLS_CLIENT_CERT')", title: "Help"}
                    %i.bi.bi-question-circle{style: "font-size: 0.75rem"}
                %input.w-full.px-3.py-2.rounded-md.border.border-gray-600.text-sm.mqtt-tls-field{class: "bg-[#0d1117]", type: "text", name: "config[mqtt][TLS_CLIENT_CERT]", value: @config.mqtt['TLS_CLIENT_CERT'], placeholder: "/etc/svxlink/mqtt-client.crt"}
              .mb-1
                %label.block.text-sm.text-gray-500.mb-1
                  TLS_CLIENT_KEY
                  %button.ml-1.text-gray-500.hover:text-blue-400.cursor-pointer{type: "button", onclick: "showHelp('MQTT_TLS_CLIENT_KEY')", title: "Help"}
                    %i.bi.bi-question-circle{style: "font-size: 0.75rem"}
                %input.w-full.px-3.py-2.rounded-md.border.border-gray-600.text-sm.mqtt-tls-field{class: "bg-[#0d1117]", type: "text", name: "config[mqtt][TLS_CLIENT_KEY]", value: @config.mqtt['TLS_CLIENT_KEY'], placeholder: "/etc/svxlink/mqtt-client.key"}

        -# ── 7. Redis (collapsed) ──
        .rounded-lg.border.border-gray-600.bg-ghcard.mb-4
          .px-4.py-3.border-b.border-gray-600.flex.items-center.justify-between.cursor-pointer{onclick: "toggleSection('redis')"}
            %h5.text-lg.font-semibold.text-gray-100
              %i.bi.bi-database.mr-2
              Redis Config Store
              %span.text-sm.font-normal.text-ghmuted.ml-2 GeuReflector
            %i.bi.bi-chevron-down.accordion-chevron.text-gray-400{id: "chevron-redis"}
          .accordion-body.px-4{id: "body-redis"}
            .p-2.rounded.border.text-xs.mb-4{class: "border-blue-700/50 text-blue-400 bg-blue-900/20"}
              %i.bi.bi-info-circle.mr-1
              When configured, [USERS], [PASSWORDS], CLUSTER_TGS, and per-trunk filters are read from Redis instead of the conf file. Leave empty to disable.

            %h6.text-sm.font-semibold.text-gray-400.mb-3 Connection
            .grid.grid-cols-1.md:grid-cols-2.gap-4.mb-4
              - { "HOST" => "127.0.0.1", "PORT" => "6379", "PASSWORD" => nil, "DB" => "0" }.each do |key, ph|
                .mb-1
                  %label.block.text-sm.text-gray-500.mb-1
                    = key
                    %button.ml-1.text-gray-500.hover:text-blue-400.cursor-pointer{type: "button", onclick: "showHelp('REDIS_#{key}')", title: "Help"}
                      %i.bi.bi-question-circle{style: "font-size: 0.75rem"}
                  %input.w-full.px-3.py-2.rounded-md.border.border-gray-600.text-sm{class: "bg-[#0d1117]", type: "text", name: "config[redis_section][#{key}]", value: @config.redis_section[key], placeholder: ph}
              .mb-1
                %label.block.text-sm.text-gray-500.mb-1
                  KEY_PREFIX
                  %button.ml-1.text-gray-500.hover:text-blue-400.cursor-pointer{type: "button", onclick: "showHelp('REDIS_KEY_PREFIX')", title: "Help"}
                    %i.bi.bi-question-circle{style: "font-size: 0.75rem"}
                %input.w-full.px-3.py-2.rounded-md.border.border-gray-600.text-sm{class: "bg-[#0d1117]", type: "text", name: "config[redis_section][KEY_PREFIX]", value: @config.redis_section['KEY_PREFIX']}
              .mb-1
                %label.block.text-sm.text-gray-500.mb-1
                  UNIX_SOCKET
                  %button.ml-1.text-gray-500.hover:text-blue-400.cursor-pointer{type: "button", onclick: "showHelp('REDIS_UNIX_SOCKET')", title: "Help"}
                    %i.bi.bi-question-circle{style: "font-size: 0.75rem"}
                %input.w-full.px-3.py-2.rounded-md.border.border-gray-600.text-sm{class: "bg-[#0d1117]", type: "text", name: "config[redis_section][UNIX_SOCKET]", value: @config.redis_section['UNIX_SOCKET'], placeholder: "/var/run/redis/redis.sock"}

            %h6.text-sm.font-semibold.text-gray-400.mb-3 TLS
            .grid.grid-cols-1.md:grid-cols-2.gap-4
              .mb-1
                %label.block.text-sm.text-gray-500.mb-1
                  TLS_ENABLED
                  %button.ml-1.text-gray-500.hover:text-blue-400.cursor-pointer{type: "button", onclick: "showHelp('REDIS_TLS_ENABLED')", title: "Help"}
                    %i.bi.bi-question-circle{style: "font-size: 0.75rem"}
                %select.w-full.px-3.py-2.rounded-md.border.border-gray-600.text-sm{class: "bg-[#0d1117]", name: "config[redis_section][TLS_ENABLED]"}
                  %option{value: "", selected: @config.redis_section['TLS_ENABLED'].blank?} (not set)
                  %option{value: "0", selected: @config.redis_section['TLS_ENABLED'] == '0'} Disabled
                  %option{value: "1", selected: @config.redis_section['TLS_ENABLED'] == '1'} Enabled
              .mb-1
                %label.block.text-sm.text-gray-500.mb-1
                  TLS_CA_CERT
                  %button.ml-1.text-gray-500.hover:text-blue-400.cursor-pointer{type: "button", onclick: "showHelp('REDIS_TLS_CA_CERT')", title: "Help"}
                    %i.bi.bi-question-circle{style: "font-size: 0.75rem"}
                %input.w-full.px-3.py-2.rounded-md.border.border-gray-600.text-sm{class: "bg-[#0d1117]", type: "text", name: "config[redis_section][TLS_CA_CERT]", value: @config.redis_section['TLS_CA_CERT']}
              .mb-1
                %label.block.text-sm.text-gray-500.mb-1
                  TLS_CLIENT_CERT
                  %button.ml-1.text-gray-500.hover:text-blue-400.cursor-pointer{type: "button", onclick: "showHelp('REDIS_TLS_CLIENT_CERT')", title: "Help"}
                    %i.bi.bi-question-circle{style: "font-size: 0.75rem"}
                %input.w-full.px-3.py-2.rounded-md.border.border-gray-600.text-sm{class: "bg-[#0d1117]", type: "text", name: "config[redis_section][TLS_CLIENT_CERT]", value: @config.redis_section['TLS_CLIENT_CERT']}
              .mb-1
                %label.block.text-sm.text-gray-500.mb-1
                  TLS_CLIENT_KEY
                  %button.ml-1.text-gray-500.hover:text-blue-400.cursor-pointer{type: "button", onclick: "showHelp('REDIS_TLS_CLIENT_KEY')", title: "Help"}
                    %i.bi.bi-question-circle{style: "font-size: 0.75rem"}
                %input.w-full.px-3.py-2.rounded-md.border.border-gray-600.text-sm{class: "bg-[#0d1117]", type: "text", name: "config[redis_section][TLS_CLIENT_KEY]", value: @config.redis_section['TLS_CLIENT_KEY']}

            %h6.text-sm.font-semibold.text-gray-400.mb-3.mt-4 Sync Config File / Redis
            .p-2.rounded.border.text-xs.mb-3{class: "border-gray-700 text-ghmuted bg-ghbg"}
              %i.bi.bi-info-circle.mr-1
              Sync [USERS], [PASSWORDS], CLUSTER_TGS, and per-trunk filters between the conf file and Redis. Safe to run multiple times (idempotent). Save your config first if you made changes.
            .flex.items-center.flex-wrap.gap-3
              %button.px-3.py-1.5.rounded-md.border.border-gray-600.text-sm.text-gray-300.hover:text-gray-100.cursor-pointer{type: "button", id: "redis-dry-run-btn", onclick: "redisSync('import', true)"}
                %i.bi.bi-eye.mr-1
                Dry Run
              %button.px-3.py-1.5.rounded-md.border.text-sm.font-semibold.cursor-pointer{class: "border-amber-600 text-amber-400 hover:bg-amber-600 hover:text-white", type: "button", id: "redis-import-btn", onclick: "redisSync('import', false)"}
                %i.bi.bi-database-add.mr-1
                File to Redis
              %button.px-3.py-1.5.rounded-md.border.text-sm.font-semibold.cursor-pointer{class: "border-cyan-600 text-cyan-400 hover:bg-cyan-600 hover:text-white", type: "button", id: "redis-export-btn", onclick: "redisSync('export', false)"}
                %i.bi.bi-file-earmark-arrow-down.mr-1
                Redis to File
            #redis-import-output.mt-3.hidden
              %pre.text-xs.p-3.rounded-lg.border.border-gray-700.overflow-x-auto.max-h-64.overflow-y-auto{class: "bg-[#0d1117] text-gray-300"}

        -# ── Save ──
        %input{type: "hidden", name: "restart", value: "0", id: "restart-input"}
        .flex.gap-2
          %button.px-4.py-2.rounded-md.border.font-semibold.cursor-pointer.transition-colors{class: "border-green-600 text-green-400", type: "submit", id: "btn-save", onclick: "document.getElementById('restart-input').value='0'"}
            %i.bi.bi-floppy.mr-1
            Save
          %button.px-4.py-2.rounded-md.border.font-semibold.cursor-pointer.transition-colors{class: "border-blue-600 text-blue-400", type: "submit", id: "btn-save-restart", onclick: "document.getElementById('restart-input').value='1'"}
            %i.bi.bi-arrow-clockwise.mr-1
            Save & Restart
          %button.px-4.py-2.rounded-md.border.border-gray-600.text-gray-400.hover:text-gray-200.cursor-pointer{type: "button", onclick: "openBackups()"}
            %i.bi.bi-clock-history.mr-1
            Backups
          = link_to "Back", admin_users_path, class: "px-4 py-2 rounded-md border border-gray-600 text-gray-400 hover:text-gray-200"

      </div>
      <div id="panel-nodeblock" style="display:none">
      -# ── Node Block (runtime, outside the form) ──
      .rounded-lg.border.border-gray-600.bg-ghcard.mb-4
        .px-4.py-3.border-b.border-gray-600.flex.items-center.justify-between.cursor-pointer{onclick: "toggleSection('nodeblock')"}
          %h5.text-lg.font-semibold.text-gray-100
            %i.bi.bi-mic-mute.mr-2
            Node Block
            %span.text-sm.font-normal.text-ghmuted.ml-2 Temporarily mute a node
          %i.bi.bi-chevron-down.accordion-chevron.text-gray-400{id: "chevron-nodeblock"}
        .accordion-body.px-4{id: "body-nodeblock"}
          %p.text-xs.text-ghmuted.mb-3
            Block a node from transmitting for a set duration. The node can still listen but cannot talk.
            The block resets each time the node tries to transmit. Set to 0 seconds to unblock.
          .flex.flex-wrap.items-end.gap-3
            .flex-1{style: "min-width: 160px"}
              %label.block.text-sm.text-gray-500.mb-1 Callsign
              %input#block-callsign.w-full.px-3.py-2.rounded-md.border.border-gray-600.text-sm{class: "bg-[#0d1117]", type: "text", placeholder: "e.g. SM0ABC-1"}
            .flex-1{style: "min-width: 140px"}
              %label.block.text-sm.text-gray-500.mb-1 Duration
              %select#block-duration.w-full.px-3.py-2.rounded-md.border.border-gray-600.text-sm{class: "bg-[#0d1117]"}
                %option{value: "0"} Unblock (0s)
                %option{value: "60"} 1 minute
                %option{value: "300", selected: true} 5 minutes
                %option{value: "600"} 10 minutes
                %option{value: "1800"} 30 minutes
                %option{value: "3600"} 1 hour
            %button#block-btn.px-4.py-2.rounded-md.bg-orange-600.hover:bg-orange-700.text-white.font-semibold.cursor-pointer.text-sm{type: "button", onclick: "blockNode()"}
              %i.bi.bi-mic-mute.mr-1
              Block
          #block-result.mt-3.text-sm

      </div>
      <div id="panel-certfiles" style="display:none">
      -# ── Pending Certificates ──
      .rounded-lg.border.border-gray-600.bg-ghcard.mb-4
        .px-4.py-3.border-b.border-gray-600.flex.items-center.justify-between.cursor-pointer{onclick: "toggleSection('csrs')"}
          %h5.text-lg.font-semibold.text-gray-100
            %i.bi.bi-shield-check.mr-2
            Pending Certificates
            %span.text-sm.font-normal.text-ghmuted.ml-2#csr-count
          .flex.items-center.gap-2
            %button.text-sm.px-3.py-1.rounded-md.border.border-gray-600.text-gray-600.cursor-not-allowed.opacity-50{type: "button", id: "btn-csrs", disabled: true, onclick: "event.stopPropagation(); loadCsrs()"}
              %i.bi.bi-arrow-clockwise.mr-1
              Refresh
            %i.bi.bi-chevron-down.accordion-chevron.text-gray-400{id: "chevron-csrs"}
        .accordion-body.px-4{id: "body-csrs"}
          #csrs-content.text-sm.text-ghmuted
            %span.text-ghdim Click Refresh to load pending certificate requests.

      -# ── Our Certificates ──
      .rounded-lg.border.border-gray-600.bg-ghcard.mb-4
        .px-4.py-3.border-b.border-gray-600.flex.items-center.justify-between.cursor-pointer{onclick: "toggleSection('ourcerts')"}
          %h5.text-lg.font-semibold.text-gray-100
            %i.bi.bi-award.mr-2
            Our Certificates
          .flex.items-center.gap-2
            %button.text-sm.px-3.py-1.rounded-md.border.border-red-800.text-gray-600.cursor-not-allowed.opacity-50{type: "button", id: "btn-reset-pki", disabled: true, onclick: "event.stopPropagation(); resetPki()"}
              %i.bi.bi-trash3.mr-1
              Reset PKI
            %a.text-sm.px-3.py-1.rounded-md.border.border-gray-600.text-gray-600.cursor-not-allowed.opacity-50{id: "btn-ourcerts", href: "#{export_ca_bundle_admin_reflector_path}", onclick: "event.stopPropagation()", style: "pointer-events:none"}
              %i.bi.bi-download.mr-1
              Export CA Bundle
            %i.bi.bi-chevron-down.accordion-chevron.text-gray-400{id: "chevron-ourcerts"}
        .accordion-body.px-4{id: "body-ourcerts"}
          #ourcerts-content.text-sm.text-ghmuted
            %span.text-ghdim Loading…
      </div>

    :javascript
      function switchTab(tab) {
        ['config', 'nodeblock', 'certfiles'].forEach(function(t) {
          document.getElementById('panel-' + t).style.display = t === tab ? '' : 'none';
        });
        document.querySelectorAll('.tab-btn').forEach(function(btn) {
          var active = btn.id === 'tab-' + tab;
          btn.classList.toggle('border-blue-500', active);
          btn.classList.toggle('text-blue-400', active);
          btn.classList.toggle('border-transparent', !active);
          btn.classList.toggle('text-gray-400', !active);
        });
        if (tab === 'nodeblock') {
          var nbBody = document.getElementById('body-nodeblock');
          if (!nbBody.classList.contains('open')) toggleSection('nodeblock');
        }
        if (tab === 'certfiles') {
          var csrBody = document.getElementById('body-csrs');
          if (!csrBody.classList.contains('open')) toggleSection('csrs');
          var ourBody = document.getElementById('body-ourcerts');
          if (!ourBody.classList.contains('open')) toggleSection('ourcerts');
        }
      }

      var HELP_TEXTS = #{help.to_json};

      function openModal(id) { var m = document.getElementById(id); m.classList.remove('hidden'); m.classList.add('flex'); }
      function closeModal(id) { var m = document.getElementById(id); m.classList.add('hidden'); m.classList.remove('flex'); }

      function showHelp(key) {
        document.getElementById('help-modal-title').textContent = key;
        document.getElementById('help-modal-body').textContent = HELP_TEXTS[key] || 'No description available.';
        openModal('help-modal');
      }
      function closeHelp() {
        closeModal('help-modal');
      }
      document.getElementById('help-modal').addEventListener('click', function(e) {
        if (e.target === this) closeHelp();
      });

      (function() {
        var flash = document.getElementById('flash-notice');
        if (flash) setTimeout(function() { flash.style.transition = 'opacity 0.5s'; flash.style.opacity = '0'; setTimeout(function() { flash.remove(); }, 500); }, 5000);
      })();

      var _confirmTarget = null;
      function confirmRemove(btn, selector) {
        _confirmTarget = btn.closest(selector);
        openModal('confirm-modal');
      }
      document.getElementById('confirm-delete').addEventListener('click', function() {
        if (_confirmTarget) _confirmTarget.remove();
        _confirmTarget = null;
        closeModal('confirm-modal');
        syncGroupSelects();
      });
      document.getElementById('confirm-cancel').addEventListener('click', function() {
        _confirmTarget = null;
        closeModal('confirm-modal');
      });
      document.getElementById('confirm-modal').addEventListener('click', function(e) {
        if (e.target === this) { _confirmTarget = null; closeModal('confirm-modal'); }
      });

      function toggleSection(id) {
        var body = document.getElementById('body-' + id);
        var chevron = document.getElementById('chevron-' + id);
        var opening = !body.classList.contains('open');
        if (opening) {
          body.style.maxHeight = body.scrollHeight + 'px';
          body.classList.add('open');
          body.addEventListener('transitionend', function handler(e) {
            if (e.propertyName === 'max-height' && body.classList.contains('open')) body.style.maxHeight = 'none';
            body.removeEventListener('transitionend', handler);
          });
        } else {
          body.style.maxHeight = body.scrollHeight + 'px';
          body.offsetHeight; // force reflow so the collapse below animates from this height
          body.classList.remove('open');
          body.style.maxHeight = '0px';
        }
        chevron.classList.toggle('open');
        var btn = document.getElementById('btn-' + id);
        if (btn) {
          var open = body.classList.contains('open');
          btn.disabled = !open;
          btn.classList.toggle('opacity-50', !open);
          btn.classList.toggle('cursor-not-allowed', !open);
          btn.classList.toggle('text-gray-600', !open);
          btn.classList.toggle('cursor-pointer', open);
          btn.classList.toggle('text-gray-400', open);
          btn.classList.toggle('hover:text-gray-200', open);
        }
        if (id === 'csrs' && body.classList.contains('open')) loadCsrs();
        if (id === 'ourcerts' && body.classList.contains('open')) loadOurCerts();
        if (id === 'ourcerts') {
          var isOpen = body.classList.contains('open');
          // Export button (link)
          var exportBtn = document.getElementById('btn-ourcerts');
          if (exportBtn) {
            exportBtn.style.pointerEvents = isOpen ? 'auto' : 'none';
            exportBtn.classList.toggle('opacity-50', !isOpen);
            exportBtn.classList.toggle('cursor-not-allowed', !isOpen);
            exportBtn.classList.toggle('text-gray-600', !isOpen);
            exportBtn.classList.toggle('cursor-pointer', isOpen);
            exportBtn.classList.toggle('text-gray-400', isOpen);
            exportBtn.classList.toggle('hover:text-gray-200', isOpen);
          }
          // Reset PKI button
          var resetBtn = document.getElementById('btn-reset-pki');
          if (resetBtn) {
            resetBtn.disabled = !isOpen;
            resetBtn.classList.toggle('opacity-50', !isOpen);
            resetBtn.classList.toggle('cursor-not-allowed', !isOpen);
            resetBtn.classList.toggle('text-gray-600', !isOpen);
            resetBtn.classList.toggle('cursor-pointer', isOpen);
            resetBtn.classList.toggle('text-red-400', isOpen);
            resetBtn.classList.toggle('hover:text-red-300', isOpen);
          }
        }
      }

      function toggleMqttTls() {
        var sel = document.getElementById('mqtt-tls-enabled');
        var enabled = sel && sel.value === '1';
        document.querySelectorAll('.mqtt-tls-field').forEach(function(el) {
          el.disabled = !enabled;
          el.classList.toggle('opacity-50', !enabled);
        });
      }
      toggleMqttTls();

      function redisSync(direction, dryRun) {
        var btnId = dryRun ? 'redis-dry-run-btn' : (direction === 'import' ? 'redis-import-btn' : 'redis-export-btn');
        var btn = document.getElementById(btnId);
        var outputWrap = document.getElementById('redis-import-output');
        var outputPre = outputWrap.querySelector('pre');
        btn.disabled = true;
        btn.classList.add('opacity-50');
        var label = dryRun ? 'Running dry run...' : (direction === 'import' ? 'Importing to Redis...' : 'Exporting to file...');
        outputPre.textContent = label;
        outputWrap.classList.remove('hidden');
        var url = direction === 'export' ? '/admin/reflector/redis_export' : '/admin/reflector/redis_import';
        fetch(url, {
          method: 'POST',
          headers: { 'Content-Type': 'application/json', 'X-CSRF-Token': document.querySelector('meta[name=csrf-token]').content },
          body: JSON.stringify({ dry_run: dryRun })
        })
        .then(function(r) { return r.json(); })
        .then(function(data) {
          if (data.error) {
            outputPre.textContent = 'Error: ' + data.error;
            outputPre.classList.add('text-red-400');
            outputPre.classList.remove('text-gray-300');
          } else {
            outputPre.textContent = data.output || '(no output)';
            outputPre.classList.remove('text-red-400');
            outputPre.classList.add('text-gray-300');
          }
        })
        .catch(function(e) {
          outputPre.textContent = 'Request failed: ' + e.message;
          outputPre.classList.add('text-red-400');
        })
        .finally(function() {
          btn.disabled = false;
          btn.classList.remove('opacity-50');
        });
      }

      // ── Save button change tracking ──
      // HOT fields: take effect via CFG pipe without restart
      var HOT_NAMES = [
        'config[user_callsigns][]', 'config[user_groups][]',
        'config[password_names][]', 'config[password_values][]',
        'config[tg_numbers][]', 'config[tg_allows][]',
        'config[tg_auto_qsys][]', 'config[tg_show_activities][]',
        'config[tg_allow_monitors][]'
      ];
      var HOT_GLOBALS = ['SQL_TIMEOUT', 'SQL_TIMEOUT_BLOCKTIME', 'ACCEPT_CALLSIGN', 'REJECT_CALLSIGN', 'CODECS'];

      function isHotField(input) {
        var name = input.name || '';
        if (HOT_NAMES.indexOf(name) !== -1) return true;
        for (var i = 0; i < HOT_GLOBALS.length; i++) {
          if (name === 'config[global][' + HOT_GLOBALS[i] + ']') return true;
        }
        return false;
      }

      // Snapshot initial form values after all init JS has run
      var _form = document.getElementById('reflector-config-form');
      var _snapshot = {};
      var _snapshotCount = 0;

      function snapshotForm() {
        _snapshot = {};
        if (!_form) return;
        _form.querySelectorAll('input:not([type=hidden]), select, textarea').forEach(function(el, i) {
          _snapshot[i + '|' + (el.name || '')] = el.value;
        });
        _snapshotCount = Object.keys(_snapshot).length;
      }

      function checkFormChanges() {
        if (!_form) return;
        var inputs = _form.querySelectorAll('input:not([type=hidden]), select, textarea');
        var hasHot = false, hasCold = false;

        inputs.forEach(function(el, i) {
          if (!el.name) return;
          var key = i + '|' + el.name;
          var orig = _snapshot[key];
          if (orig !== undefined && el.value !== orig) {
            if (isHotField(el)) hasHot = true;
            else hasCold = true;
          }
        });

        // Detect added/removed rows (new trunk peer, user, TG rule, etc.)
        if (inputs.length !== _snapshotCount) hasCold = true;

        var btnSave = document.getElementById('btn-save');
        var btnRestart = document.getElementById('btn-save-restart');
        if (hasCold) {
          btnSave.className = 'px-4 py-2 rounded-md border font-semibold cursor-pointer transition-colors border-green-600 text-green-400';
          btnRestart.className = 'px-4 py-2 rounded-md border font-semibold cursor-pointer transition-colors border-blue-600 bg-blue-600 text-white';
        } else if (hasHot) {
          btnSave.className = 'px-4 py-2 rounded-md border font-semibold cursor-pointer transition-colors border-green-600 bg-green-600 text-white';
          btnRestart.className = 'px-4 py-2 rounded-md border font-semibold cursor-pointer transition-colors border-blue-600 text-blue-400';
        } else {
          btnSave.className = 'px-4 py-2 rounded-md border font-semibold cursor-pointer transition-colors border-green-600 text-green-400';
          btnRestart.className = 'px-4 py-2 rounded-md border font-semibold cursor-pointer transition-colors border-blue-600 text-blue-400';
        }
      }

      if (_form) {
        _form.addEventListener('input', checkFormChanges);
        _form.addEventListener('change', checkFormChanges);
        // Watch for dynamic row add/remove
        var _observer = new MutationObserver(checkFormChanges);
        _observer.observe(_form, { childList: true, subtree: true });
      }

      function getPasswordGroups() {
        var groups = [];
        document.querySelectorAll('#passwords-tbody input[name="config[password_names][]"]').forEach(function(el) {
          var v = el.value.trim();
          if (v) groups.push(v);
        });
        return groups;
      }

      function syncGroupSelects() {
        var groups = getPasswordGroups();
        document.querySelectorAll('.user-group-select').forEach(function(sel) {
          var current = sel.value || sel.dataset.selected || '';
          sel.innerHTML = '';
          groups.forEach(function(g) {
            var opt = document.createElement('option');
            opt.value = g;
            opt.textContent = g;
            if (g === current) opt.selected = true;
            sel.appendChild(opt);
          });
          delete sel.dataset.selected;
        });
      }

      document.getElementById('passwords-tbody').addEventListener('input', function(e) {
        if (e.target.name === 'config[password_names][]') syncGroupSelects();
      });

      syncGroupSelects();
      // Snapshot AFTER all init JS (syncGroupSelects, toggleMqttTls, etc.)
      snapshotForm();

      function addUserRow() {
        var tbody = document.getElementById('users-tbody');
        var tr = document.createElement('tr');
        tr.className = 'border-t border-gray-700';
        tr.innerHTML = '<td class="py-2 pr-2"><input class="w-full px-2 py-1 rounded border border-gray-600 text-sm bg-[#0d1117]" type="text" name="config[user_callsigns][]" placeholder="CALLSIGN"></td>' +
          '<td class="py-2 pr-2"><select class="user-group-select w-full px-2 py-1 rounded border border-gray-600 text-sm bg-[#0d1117]" name="config[user_groups][]"></select></td>' +
          '<td class="py-2 text-center"><button type="button" class="text-red-400 hover:text-red-300 cursor-pointer" onclick="confirmRemove(this,\'tr\')"><i class="bi bi-x-lg"></i></button></td>';
        tbody.insertBefore(tr, tbody.firstChild);
        syncGroupSelects();
        var body = document.getElementById('body-users');
        if (!body.classList.contains('open')) toggleSection('users');
      }

      function addPasswordRow() {
        var tbody = document.getElementById('passwords-tbody');
        var tr = document.createElement('tr');
        tr.className = 'border-t border-gray-700';
        tr.innerHTML = '<td class="py-2 pr-2"><input class="w-full px-2 py-1 rounded border border-gray-600 text-sm bg-[#0d1117]" type="text" name="config[password_names][]" placeholder="group_name"></td>' +
          '<td class="py-2 pr-2"><input class="w-full px-2 py-1 rounded border border-gray-600 text-sm bg-[#0d1117]" type="text" name="config[password_values][]" placeholder="password"></td>' +
          '<td class="py-2 text-center"><button type="button" class="text-red-400 hover:text-red-300 cursor-pointer" onclick="confirmRemove(this,\'tr\')"><i class="bi bi-x-lg"></i></button></td>';
        tbody.appendChild(tr);
        syncGroupSelects();
        var body = document.getElementById('body-passwords');
        if (!body.classList.contains('open')) toggleSection('passwords');
      }

      function setTrunkMode(mode) {
        var refPanel = document.getElementById('trunk-mode-reflector');
        var satPanel = document.getElementById('trunk-mode-satellite');
        var refBtn = document.getElementById('mode-btn-reflector');
        var satBtn = document.getElementById('mode-btn-satellite');
        var addBtn = document.getElementById('btn-trunking');
        var modeInput = document.getElementById('trunk-mode-input');
        if (modeInput) modeInput.value = mode;
        if (mode === 'satellite') {
          refPanel.classList.add('hidden');
          satPanel.classList.remove('hidden');
          refBtn.className = 'px-4 py-2 text-sm font-semibold cursor-pointer transition-colors text-gray-400 hover:text-gray-200';
          satBtn.className = 'px-4 py-2 text-sm font-semibold cursor-pointer transition-colors border-l border-gray-600 bg-purple-600 text-white';
          if (addBtn) { addBtn.style.display = 'none'; }
        } else {
          refPanel.classList.remove('hidden');
          satPanel.classList.add('hidden');
          refBtn.className = 'px-4 py-2 text-sm font-semibold cursor-pointer transition-colors bg-blue-600 text-white';
          satBtn.className = 'px-4 py-2 text-sm font-semibold cursor-pointer transition-colors border-l border-gray-600 text-gray-400 hover:text-gray-200';
          if (addBtn) { addBtn.style.display = ''; }
        }
      }

      function addTrunkPeer() {
        var container = document.getElementById('trunk-peers');
        var div = document.createElement('div');
        div.className = 'trunk-peer border border-gray-700 rounded-lg p-4 relative';
        div.innerHTML = '<button type="button" class="absolute top-2 right-2 text-red-400 hover:text-red-300 cursor-pointer" onclick="confirmRemove(this,\'.trunk-peer\')"><i class="bi bi-x-lg"></i></button>' +
          '<div class="grid grid-cols-1 md:grid-cols-2 gap-3">' +
            '<div class="col-span-1"><label class="block text-sm text-gray-500 mb-1">Section Name <button type="button" class="ml-1 text-gray-500 hover:text-blue-400 cursor-pointer" onclick="showHelp(\'TRUNK_SECTION_NAME\')" title="Help"><i class="bi bi-question-circle" style="font-size:0.75rem"></i></button></label><div class="flex items-center gap-0"><span class="px-2 py-1 rounded-l border border-r-0 border-gray-600 text-sm text-gray-500 bg-[#161b22]">TRUNK_</span><input class="w-full px-2 py-1 rounded-r border border-gray-600 text-sm uppercase bg-[#0d1117]" type="text" name="config[trunk_names][]" placeholder="REFA_REFB"></div></div>' +
            '<div class="col-span-1"><label class="block text-sm text-gray-500 mb-1">HOST</label><input class="w-full px-2 py-1 rounded border border-gray-600 text-sm bg-[#0d1117]" type="text" name="config[trunk_hosts][]" placeholder="reflector-b.example.com"></div>' +
            '<div class="col-span-1"><label class="block text-sm text-gray-500 mb-1">PORT</label><input class="w-full px-2 py-1 rounded border border-gray-600 text-sm bg-[#0d1117]" type="text" name="config[trunk_ports][]" placeholder="5302"></div>' +
            '<div class="col-span-1"><label class="block text-sm text-gray-500 mb-1">SECRET</label><input class="w-full px-2 py-1 rounded border border-gray-600 text-sm bg-[#0d1117]" type="text" name="config[trunk_secrets][]"></div>' +
            '<div class="col-span-2"><label class="block text-sm text-gray-500 mb-1">REMOTE_PREFIX (comma-separated)</label><input class="w-full px-2 py-1 rounded border border-gray-600 text-sm bg-[#0d1117]" type="text" name="config[trunk_remote_prefixes][]" placeholder="2,3"></div>' +
            '<div class="col-span-2"><label class="block text-sm text-gray-500 mb-1">Remote status URL</label><input class="w-full px-2 py-1 rounded border border-gray-600 text-sm bg-[#0d1117]" type="url" name="config[trunk_status_urls][]" placeholder="http://reflector-b.example.com:8080/status"></div>' +
            '<div class="col-span-2"><details class="text-xs"><summary class="text-gray-500 cursor-pointer hover:text-gray-300">Advanced trunk options</summary><div class="grid grid-cols-1 md:grid-cols-2 gap-3 mt-3">' +
              '<div class="col-span-1"><label class="block text-sm text-gray-500 mb-1">PEER_ID <button type="button" class="ml-1 text-gray-500 hover:text-blue-400 cursor-pointer" onclick="showHelp(\'TRUNK_PEER_ID\')" title="Help"><i class="bi bi-question-circle" style="font-size:0.75rem"></i></button></label><input class="w-full px-2 py-1 rounded border border-gray-600 text-sm bg-[#0d1117]" type="text" name="config[trunk_peer_ids][]"></div>' +
              '<div class="col-span-1"><label class="block text-sm text-gray-500 mb-1">PAIRED (twin peer) <button type="button" class="ml-1 text-gray-500 hover:text-blue-400 cursor-pointer" onclick="showHelp(\'TRUNK_PAIRED\')" title="Help"><i class="bi bi-question-circle" style="font-size:0.75rem"></i></button></label><select class="w-full px-2 py-1 rounded border border-gray-600 text-sm bg-[#0d1117]" name="config[trunk_paireds][]"><option value="0">No</option><option value="1">Yes</option></select></div>' +
              '<div class="col-span-1"><label class="block text-sm text-gray-500 mb-1">BLACKLIST_TGS <button type="button" class="ml-1 text-gray-500 hover:text-blue-400 cursor-pointer" onclick="showHelp(\'TRUNK_BLACKLIST_TGS\')" title="Help"><i class="bi bi-question-circle" style="font-size:0.75rem"></i></button></label><input class="w-full px-2 py-1 rounded border border-gray-600 text-sm bg-[#0d1117]" type="text" name="config[trunk_blacklist_tgs][]" placeholder="666,777*,100-199"></div>' +
              '<div class="col-span-1"><label class="block text-sm text-gray-500 mb-1">ALLOW_TGS <button type="button" class="ml-1 text-gray-500 hover:text-blue-400 cursor-pointer" onclick="showHelp(\'TRUNK_ALLOW_TGS\')" title="Help"><i class="bi bi-question-circle" style="font-size:0.75rem"></i></button></label><input class="w-full px-2 py-1 rounded border border-gray-600 text-sm bg-[#0d1117]" type="text" name="config[trunk_allow_tgs][]"></div>' +
              '<div class="col-span-2"><label class="block text-sm text-gray-500 mb-1">TG_MAP <button type="button" class="ml-1 text-gray-500 hover:text-blue-400 cursor-pointer" onclick="showHelp(\'TRUNK_TG_MAP\')" title="Help"><i class="bi bi-question-circle" style="font-size:0.75rem"></i></button></label><input class="w-full px-2 py-1 rounded border border-gray-600 text-sm bg-[#0d1117]" type="text" name="config[trunk_tg_maps][]" placeholder="100:200,101:201"></div>' +
              '<div class="col-span-2"><label class="block text-sm text-gray-500 mb-1">ROUTABLE_PREFIXES (comma-separated) <button type="button" class="ml-1 text-gray-500 hover:text-blue-400 cursor-pointer" onclick="showHelp(\'TRUNK_ROUTABLE_PREFIXES\')" title="Help"><i class="bi bi-question-circle" style="font-size:0.75rem"></i></button></label><input class="w-full px-2 py-1 rounded border border-gray-600 text-sm bg-[#0d1117]" type="text" name="config[trunk_routable_prefixes][]" placeholder="9,72"></div>' +
            '</div></details></div>' +
          '</div>';
        container.appendChild(div);
        var body = document.getElementById('body-trunking');
        if (!body.classList.contains('open')) toggleSection('trunking');
      }

      function addSatelliteSecret() {
        var container = document.getElementById('satellite-secret-rows');
        var row = document.createElement('div');
        row.className = 'satellite-secret-row grid grid-cols-12 gap-2 mb-2';
        row.innerHTML =
          '<div class="col-span-4"><input class="w-full px-3 py-2 rounded-md border border-gray-600 text-sm bg-[#0d1117]" type="text" name="config[satellite_secret_ids][]" pattern="[A-Za-z0-9\\-]+" placeholder="satellite-id"></div>' +
          '<div class="col-span-7"><input class="w-full px-3 py-2 rounded-md border border-gray-600 text-sm bg-[#0d1117]" type="text" name="config[satellite_secret_values][]" placeholder="secret"></div>' +
          '<div class="col-span-1 flex items-center"><button type="button" class="text-red-400 hover:text-red-300 cursor-pointer" onclick="removeSatelliteSecret(this, null)" title="Remove"><i class="bi bi-x-lg"></i></button></div>';
        container.appendChild(row);
      }

      function removeSatelliteSecret(btn, id) {
        var row = btn.closest('.satellite-secret-row');
        if (!id) {
          row.parentNode.removeChild(row);
          return;
        }
        showDanger(
          'Remove SECRET_' + id + '?',
          'The "' + id + '" satellite will be unable to reconnect after the next reflector restart unless a fallback SECRET is configured.',
          'Remove',
          function() { row.parentNode.removeChild(row); }
        );
      }

      // Auto-prefill satellite parent status URL from SATELLITE_OF
      (function() {
        var satHost = document.getElementById('satellite-of-input');
        var satUrl = document.getElementById('satellite-status-url-input');
        if (satHost && satUrl) {
          satHost.addEventListener('input', function() {
            if (satUrl.dataset.userEdited) return;
            var host = satHost.value.trim();
            satUrl.value = host ? 'https://' + host + '/status' : '';
          });
          satUrl.addEventListener('input', function() {
            satUrl.dataset.userEdited = 'true';
          });
        }
      })();

      // Auto-prefill Remote status URL from HOST
      document.getElementById('trunk-peers').addEventListener('input', function(e) {
        var input = e.target;
        if (input.name === 'config[trunk_hosts][]') {
          var row = input.closest('.trunk-peer');
          if (!row) return;
          var urlInput = row.querySelector('input[name="config[trunk_status_urls][]"]');
          if (!urlInput || urlInput.dataset.userEdited) return;
          var host = input.value.trim();
          urlInput.value = host ? 'https://' + host + '/status' : '';
        } else if (input.name === 'config[trunk_status_urls][]') {
          input.dataset.userEdited = 'true';
        }
      });

      function addTgRule() {
        var container = document.getElementById('tg-rules');
        var div = document.createElement('div');
        div.className = 'tg-rule border border-gray-700 rounded-lg p-4 relative';
        div.innerHTML = '<button type="button" class="absolute top-2 right-2 text-red-400 hover:text-red-300 cursor-pointer" onclick="confirmRemove(this,\'.tg-rule\')"><i class="bi bi-x-lg"></i></button>' +
          '<div class="grid grid-cols-1 md:grid-cols-2 gap-3">' +
            '<div class="col-span-1"><label class="block text-sm text-gray-500 mb-1">TG Number</label><input class="w-full px-2 py-1 rounded border border-gray-600 text-sm bg-[#0d1117]" type="number" name="config[tg_numbers][]" min="1" placeholder="1"></div>' +
            '<div class="col-span-1"><label class="block text-sm text-gray-500 mb-1">ALLOW (regex) <button type="button" class="ml-1 text-gray-500 hover:text-blue-400 cursor-pointer" onclick="showHelp(\'ALLOW\')" title="Help"><i class="bi bi-question-circle" style="font-size:0.75rem"></i></button></label><input class="w-full px-2 py-1 rounded border border-gray-600 text-sm bg-[#0d1117]" type="text" name="config[tg_allows][]" placeholder="^.*$"></div>' +
            '<div class="col-span-1"><label class="block text-sm text-gray-500 mb-1">AUTO_QSY_AFTER (seconds) <button type="button" class="ml-1 text-gray-500 hover:text-blue-400 cursor-pointer" onclick="showHelp(\'AUTO_QSY_AFTER\')" title="Help"><i class="bi bi-question-circle" style="font-size:0.75rem"></i></button></label><input class="w-full px-2 py-1 rounded border border-gray-600 text-sm bg-[#0d1117]" type="number" name="config[tg_auto_qsys][]"></div>' +
            '<div class="col-span-1"><label class="block text-sm text-gray-500 mb-1">SHOW_ACTIVITY <button type="button" class="ml-1 text-gray-500 hover:text-blue-400 cursor-pointer" onclick="showHelp(\'SHOW_ACTIVITY\')" title="Help"><i class="bi bi-question-circle" style="font-size:0.75rem"></i></button></label><select class="w-full px-2 py-1 rounded border border-gray-600 text-sm bg-[#0d1117]" name="config[tg_show_activities][]"><option value="">(not set)</option><option value="1">Yes</option><option value="0">No</option></select></div>' +
            '<div class="col-span-1"><label class="block text-sm text-gray-500 mb-1">ALLOW_MONITOR (regex) <button type="button" class="ml-1 text-gray-500 hover:text-blue-400 cursor-pointer" onclick="showHelp(\'ALLOW_MONITOR\')" title="Help"><i class="bi bi-question-circle" style="font-size:0.75rem"></i></button></label><input class="w-full px-2 py-1 rounded border border-gray-600 text-sm bg-[#0d1117]" type="text" name="config[tg_allow_monitors][]" placeholder="^.*$"></div>' +
          '</div>';
        container.appendChild(div);
        var body = document.getElementById('body-tg');
        if (!body.classList.contains('open')) toggleSection('tg');
      }

      // ── Backup browser ──
      var _backupData = null;
      function openBackups() {
        openModal('backup-modal');
        if (_backupData) { renderBackupList(_backupData); return; }
        fetch('/admin/reflector/backups')
          .then(function(r) { return r.json(); })
          .then(function(data) { _backupData = data; renderBackupList(data); })
          .catch(function() {
            document.getElementById('backup-list-inner').innerHTML = '<span class="text-red-400">Failed to load backups</span>';
          });
      }
      function closeBackups() {
        closeModal('backup-modal');
      }
      document.getElementById('backup-modal').addEventListener('click', function(e) {
        if (e.target === this) closeBackups();
      });
      function renderBackupList(backups) {
        var list = document.getElementById('backup-list-inner');
        if (!backups.length) { list.innerHTML = '<span class="text-ghdim">No backups available</span>'; return; }
        list.innerHTML = '';
        backups.forEach(function(b, i) {
          var btn = document.createElement('button');
          btn.type = 'button';
          btn.className = 'w-full text-left px-3 py-2 rounded-md text-sm hover:bg-ghborder2 cursor-pointer transition-colors text-gray-300 backup-item';
          btn.innerHTML = '<div class="font-mono text-xs">' + b.label + '</div>';
          btn.addEventListener('click', function() {
            document.querySelectorAll('.backup-item').forEach(function(el) { el.classList.remove('bg-ghborder2', 'text-gray-100'); });
            btn.classList.add('bg-ghborder2', 'text-gray-100');
            document.getElementById('backup-content-pre').textContent = b.content;
          });
          list.appendChild(btn);
          if (i === 0) btn.click();
        });
      }

      // ── Pending CSRs ──
      function loadCsrs() {
        var content = document.getElementById('csrs-content');
        content.innerHTML = '<span class="text-ghdim">Loading…</span>';
        fetch('#{pending_csrs_admin_reflector_path}')
          .then(function(r) { return r.json(); })
          .then(function(csrs) { renderCsrs(csrs); })
          .catch(function() { content.innerHTML = '<span class="text-red-400">Failed to load CSRs</span>'; });
      }
      function esc(s) { return String(s||'').replace(/&/g,'&amp;').replace(/</g,'&lt;').replace(/>/g,'&gt;').replace(/"/g,'&quot;'); }

      function renderCsrs(csrs) {
        var content = document.getElementById('csrs-content');
        var count = document.getElementById('csr-count');
        count.textContent = csrs.length > 0 ? '(' + csrs.length + ' pending)' : '';
        if (!csrs.length) {
          content.innerHTML = '<div class="flex items-center gap-2 text-green-400 py-2"><i class="bi bi-check-circle"></i> No pending certificate requests.</div>';
          return;
        }
        var html = '<div class="space-y-2">';
        csrs.forEach(function(csr) {
          var subjectHtml = csr.subject ? '<span class="text-ghmuted text-xs ml-2">' + esc(csr.subject) + '</span>' : '';
          var dateHtml = csr.date ? '<span class="text-ghdim text-xs">' + esc(csr.date) + '</span>' : '';
          html += '<div class="border border-gray-700 rounded-lg px-4 py-3" id="csr-row-' + esc(csr.callsign) + '">' +
            '<div class="flex items-center justify-between">' +
              '<div class="flex items-center gap-3 min-w-0">' +
                '<i class="bi bi-file-earmark-lock text-yellow-400 shrink-0"></i>' +
                '<div class="min-w-0">' +
                  '<div class="flex items-center gap-2 flex-wrap">' +
                    '<span class="font-mono font-semibold text-gray-200">' + esc(csr.callsign) + '</span>' +
                    dateHtml +
                  '</div>' +
                  (csr.subject ? '<div class="text-xs text-ghmuted truncate mt-0.5">' + esc(csr.subject) + '</div>' : '') +
                '</div>' +
              '</div>' +
              '<div class="flex items-center gap-2 shrink-0 ml-3">' +
                '<button type="button" class="p-2 rounded-md text-gray-400 hover:text-gray-200 hover:bg-ghborder2 cursor-pointer" onclick="inspectCsr(\'' + esc(csr.callsign) + '\')" title="Inspect">' +
                  '<i class="bi bi-eye"></i>' +
                '</button>' +
                '<button type="button" class="p-2 rounded-md text-green-400 hover:text-green-300 hover:bg-ghborder2 cursor-pointer" onclick="signCsr(\'' + esc(csr.callsign) + '\', this)" title="Sign">' +
                  '<i class="bi bi-check-lg"></i>' +
                '</button>' +
                '<button type="button" class="p-2 rounded-md text-red-400 hover:text-red-300 hover:bg-ghborder2 cursor-pointer" onclick="rejectCsr(\'' + esc(csr.callsign) + '\', this)" title="Reject">' +
                  '<i class="bi bi-trash3"></i>' +
                '</button>' +
              '</div>' +
            '</div>' +
          '</div>';
        });
        html += '</div>';
        content.innerHTML = html;
      }

      function inspectCsr(callsign) {
        var modal = document.getElementById('csr-modal');
        document.getElementById('csr-modal-title').textContent = callsign;
        document.getElementById('csr-modal-body').innerHTML = '<span class="text-ghdim">Loading…</span>';
        openModal('csr-modal');
        fetch('#{inspect_csr_admin_reflector_path}?callsign=' + encodeURIComponent(callsign))
          .then(function(r) { return r.json(); })
          .then(function(data) {
            if (data.error) {
              document.getElementById('csr-modal-body').innerHTML = '<span class="text-red-400">' + esc(data.error) + '</span>';
              return;
            }
            var html = '<div class="space-y-3">';
            if (data.subject) {
              html += '<div><div class="text-ghmuted text-xs uppercase tracking-wide mb-1">Subject</div>' +
                '<div class="text-gray-200 text-sm font-mono whitespace-pre">' + esc(data.subject) + '</div></div>';
            }
            if (data.key_info) {
              html += '<div><div class="text-ghmuted text-xs uppercase tracking-wide mb-1">Public Key</div>' +
                '<div class="text-gray-300 text-xs font-mono whitespace-pre">' + esc(data.key_info) + '</div></div>';
            }
            html += '</div>';
            document.getElementById('csr-modal-body').innerHTML = html;
          })
          .catch(function() {
            document.getElementById('csr-modal-body').innerHTML = '<span class="text-red-400">Failed to load CSR details</span>';
          });
      }
      function closeCsrModal() {
        closeModal('csr-modal');
      }
      document.getElementById('csr-modal').addEventListener('click', function(e) {
        if (e.target === this) closeCsrModal();
      });

      function csrAction(url, callsign, btn, successIcon, successLabel) {
        btn.disabled = true;
        var token = document.querySelector('meta[name="csrf-token"]');
        fetch(url, {
          method: 'POST',
          headers: { 'Content-Type': 'application/json', 'X-CSRF-Token': token ? token.content : '' },
          body: JSON.stringify({ callsign: callsign })
        })
        .then(function(r) { return r.json(); })
        .then(function(data) {
          if (data.ok) {
            var row = document.getElementById('csr-row-' + callsign);
            if (row) row.innerHTML =
              '<div class="flex items-center gap-3"><i class="bi ' + successIcon + '"></i>' +
              '<span class="font-mono font-semibold text-gray-200">' + data.callsign + '</span></div>' +
              '<span class="text-sm">' + successLabel + '</span>';
          } else {
            btn.disabled = false;
            alert('Error: ' + (data.error || 'Unknown error'));
          }
        })
        .catch(function() { btn.disabled = false; alert('Operation failed'); });
      }
      function signCsr(callsign, btn) {
        csrAction('#{sign_csr_admin_reflector_path}', callsign, btn, 'bi-check-circle text-green-400', '<i class="bi bi-check-lg mr-1 text-green-400"></i><span class="text-green-400">Signed</span>');
      }
      function rejectCsr(callsign, btn) {
        showDanger(
          'Reject CSR for "' + callsign + '"?',
          'This will delete the pending certificate request. The node will need to reconnect to submit a new one.',
          'Reject',
          function() {
            csrAction('#{reject_csr_admin_reflector_path}', callsign, btn, 'bi-x-circle text-red-400', '<i class="bi bi-x-lg mr-1 text-red-400"></i><span class="text-red-400">Rejected</span>');
          }
        );
      }

      // ── Node Block ──
      function blockNode() {
        var callsign = document.getElementById('block-callsign').value.trim();
        var seconds = parseInt(document.getElementById('block-duration').value);
        var result = document.getElementById('block-result');
        var btn = document.getElementById('block-btn');
        if (!callsign) { result.innerHTML = '<span class="text-red-400">Enter a callsign</span>'; return; }
        btn.disabled = true;
        var token = document.querySelector('meta[name="csrf-token"]');
        fetch('#{block_node_admin_reflector_path}', {
          method: 'POST',
          headers: { 'Content-Type': 'application/json', 'X-CSRF-Token': token ? token.content : '' },
          body: JSON.stringify({ callsign: callsign, seconds: seconds })
        })
        .then(function(r) { return r.json(); })
        .then(function(data) {
          btn.disabled = false;
          if (data.ok) {
            if (data.seconds === 0) {
              result.innerHTML = '<span class="text-green-400"><i class="bi bi-mic mr-1"></i>' + esc(data.callsign) + ' unblocked</span>';
            } else {
              result.innerHTML = '<span class="text-orange-400"><i class="bi bi-mic-mute mr-1"></i>' + esc(data.callsign) + ' blocked for ' + data.seconds + 's</span>';
            }
          } else {
            result.innerHTML = '<span class="text-red-400">' + esc(data.error || 'Unknown error') + '</span>';
          }
        })
        .catch(function() { btn.disabled = false; result.innerHTML = '<span class="text-red-400">Request failed</span>'; });
      }

      // ── Revoke Certificate ──
      function revokeCert(callsign, btn) {
        showDanger(
          'Revoke certificate for "' + callsign + '"?',
          'This will remove all certificate files for this node. Note: if the node already has a valid certificate cached locally, it can still connect until it expires. Use REJECT_CALLSIGN to fully block it.',
          'Revoke',
          function() {
            btn.disabled = true;
            var token = document.querySelector('meta[name="csrf-token"]');
            fetch('#{revoke_cert_admin_reflector_path}', {
              method: 'POST',
              headers: { 'Content-Type': 'application/json', 'X-CSRF-Token': token ? token.content : '' },
              body: JSON.stringify({ callsign: callsign })
            })
            .then(function(r) { return r.json(); })
            .then(function(data) {
              if (data.ok) {
                var row = document.getElementById('cert-row-' + callsign);
                if (row) row.innerHTML =
                  '<div class="flex items-center gap-2 py-2">' +
                    '<i class="bi bi-x-circle text-red-400"></i>' +
                    '<span class="font-mono font-semibold text-gray-200">' + esc(data.callsign) + '</span>' +
                    '<span class="text-red-400 text-sm">Revoked</span>' +
                  '</div>';
              } else {
                btn.disabled = false;
                alert('Error: ' + (data.error || 'Unknown error'));
              }
            })
            .catch(function() { btn.disabled = false; alert('Revoke failed'); });
          }
        );
      }

      // ── Danger confirm modal ──
      var _dangerCallback = null;
      function showDanger(title, body, confirmLabel, callback) {
        document.getElementById('danger-title').textContent = title;
        document.getElementById('danger-body').textContent = body;
        document.getElementById('danger-confirm-label').textContent = confirmLabel;
        _dangerCallback = callback;
        document.getElementById('danger-modal').classList.remove('hidden');
        document.getElementById('danger-modal').classList.add('flex');
      }
      function closeDanger() {
        document.getElementById('danger-modal').classList.add('hidden');
        document.getElementById('danger-modal').classList.remove('flex');
        _dangerCallback = null;
      }
      document.getElementById('danger-confirm').addEventListener('click', function() {
        if (_dangerCallback) _dangerCallback();
        closeDanger();
      });
      document.getElementById('danger-cancel').addEventListener('click', closeDanger);
      document.getElementById('danger-modal').addEventListener('click', function(e) {
        if (e.target === this) closeDanger();
      });

      // ── Reset PKI ──
      function resetPki() {
        showDanger(
          'Reset all certificates?',
          'This will delete all certificates and restart the reflector. All connected nodes will need to reconnect and have their CSRs re-signed.',
          'Reset PKI',
          function() {
            var btn = document.getElementById('btn-reset-pki');
            btn.disabled = true;
            btn.innerHTML = '<i class="bi bi-hourglass-split mr-1"></i>Resetting…';
            var token = document.querySelector('meta[name="csrf-token"]');
            fetch('#{reset_pki_admin_reflector_path}', {
              method: 'POST',
              headers: { 'Content-Type': 'application/json', 'X-CSRF-Token': token ? token.content : '' }
            })
            .then(function(r) { return r.json(); })
            .then(function(data) {
              if (data.ok) {
                window.location.reload();
                return;
              }
              btn.innerHTML = '<i class="bi bi-trash3 mr-1"></i>Reset PKI';
              btn.disabled = false;
            })
            .catch(function() { btn.innerHTML = '<i class="bi bi-trash3 mr-1"></i>Reset PKI'; btn.disabled = false; });
          }
        );
      }

      // ── Our Certificates ──
      var _ourCertsLoaded = false;
      function loadOurCerts() {
        if (_ourCertsLoaded) return;
        var content = document.getElementById('ourcerts-content');
        content.innerHTML = '<span class="text-ghdim">Loading…</span>';
        fetch('#{certificates_admin_reflector_path}')
          .then(function(r) { return r.json(); })
          .then(function(certs) { _ourCertsLoaded = true; renderOurCerts(certs); })
          .catch(function() { content.innerHTML = '<span class="text-red-400">Failed to load certificates</span>'; });
      }
      function renderOurCerts(certs) {
        var content = document.getElementById('ourcerts-content');
        if (!certs.length) {
          content.innerHTML = '<div class="text-ghdim py-2">No certificates found.</div>';
          return;
        }
        var infraCerts = ['svxreflector_root_ca', 'svxreflector_issuing_ca', 'svxreflector_server'];
        var labels = {
          'svxreflector_root_ca': { icon: 'bi-shield-lock', label: 'Root CA', color: 'text-yellow-400' },
          'svxreflector_issuing_ca': { icon: 'bi-shield-check', label: 'Issuing CA', color: 'text-blue-400' },
          'svxreflector_server': { icon: 'bi-hdd-rack', label: 'Server Certificate', color: 'text-green-400' }
        };
        var html = '<div class="space-y-3">';
        certs.forEach(function(cert) {
          var isClient = infraCerts.indexOf(cert.name) === -1;
          var meta = labels[cert.name] || { icon: 'bi-person-badge', label: cert.name, color: 'text-cyan-400' };
          var f = cert.fields;
          var subject = (f.subject || '').replace(/^.*?=/, '');
          var issuer = (f.issuer || '').replace(/^.*?=/, '');
          var notBefore = f.notBefore || '';
          var notAfter = f.notAfter || '';
          var serial = f.serial || '';
          var san = '';
          Object.keys(f).forEach(function(k) {
            if (k.indexOf('Subject Alternative') >= 0 || k.indexOf('subjectAltName') >= 0) san = f[k];
          });
          // Check if cert might be in the SAN fields value
          if (!san) {
            Object.keys(f).forEach(function(k) {
              if (k.indexOf('DNS:') >= 0 || k.indexOf('IP Address:') >= 0) san = k + (f[k] ? ' ' + f[k] : '');
            });
          }

          var revokeBtn = isClient ?
            '<button type="button" class="ml-auto p-1.5 rounded-md text-red-400 hover:text-red-300 hover:bg-ghborder2 cursor-pointer" onclick="revokeCert(\'' + esc(cert.name) + '\', this)" title="Revoke certificate">' +
              '<i class="bi bi-trash3"></i>' +
            '</button>' : '';
          html += '<div class="border border-gray-700 rounded-lg p-4" id="cert-row-' + esc(cert.name) + '">' +
            '<div class="flex items-center gap-2 mb-3">' +
              '<i class="bi ' + meta.icon + ' ' + meta.color + '"></i>' +
              '<span class="font-semibold text-gray-200">' + esc(meta.label) + '</span>' +
              '<span class="text-xs text-ghdim font-mono">(' + esc(cert.name) + ')</span>' +
              revokeBtn +
            '</div>' +
            '<div class="grid grid-cols-1 md:grid-cols-2 gap-x-6 gap-y-1 text-xs">' +
              '<div><span class="text-ghmuted">Subject:</span> <span class="text-gray-300 font-mono">' + esc(subject) + '</span></div>' +
              '<div><span class="text-ghmuted">Issuer:</span> <span class="text-gray-300 font-mono">' + esc(issuer) + '</span></div>' +
              '<div><span class="text-ghmuted">Valid from:</span> <span class="text-gray-300">' + esc(notBefore) + '</span></div>' +
              '<div><span class="text-ghmuted">Valid until:</span> <span class="text-gray-300">' + esc(notAfter) + '</span></div>' +
              '<div class="md:col-span-2"><span class="text-ghmuted">Serial:</span> <span class="text-gray-300 font-mono text-xs">' + esc(serial) + '</span></div>' +
              (san ? '<div class="md:col-span-2"><span class="text-ghmuted">SAN:</span> <span class="text-gray-300 font-mono text-xs">' + esc(san) + '</span></div>' : '') +
            '</div>' +
          '</div>';
        });
        html += '</div>';
        content.innerHTML = html;
      }
