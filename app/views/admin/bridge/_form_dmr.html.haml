- if @bridge.errors.any?
  .p-3.rounded.border.border-red-700.text-red-400.text-sm.mb-4{class: "bg-red-900/30"}
    %p.font-semibold.mb-1= "#{@bridge.errors.count} error(s):"
    %ul.list-disc.ml-4
      - @bridge.errors.full_messages.each do |msg|
        %li= msg

= form_with model: [:admin, @bridge], local: true do |f|
  = f.hidden_field :bridge_type, value: "dmr"

  -# ── Name ──
  .rounded-lg.border.border-gray-600.bg-ghcard.mb-4.p-4
    %label.block.text-sm.font-semibold.text-gray-400.mb-2 Bridge Name
    = f.text_field :name, class: "w-full px-3 py-2 rounded-md border border-gray-600 text-sm bg-[#0d1117] text-ghtext", placeholder: "e.g. BrandMeister TG 310"
    - if @bridge.persisted?
      #name-warn.p-2.rounded.border.text-sm.mt-2.items-center.gap-2.hidden{class: "border-yellow-700/50 text-yellow-400 bg-yellow-900/20"}
        %i.bi.bi-exclamation-triangle.mr-1
        Changing the name will recreate the container.

  -# ── DMR Network ──
  .rounded-lg.border.border-gray-600.bg-ghcard.mb-4
    .px-4.py-3.border-b.border-gray-600.flex.items-center.justify-between.cursor-pointer{onclick: "toggleSection('dmr')"}
      %h5.text-lg.font-semibold.text-gray-100
        %i.bi.bi-broadcast-pin.mr-2
        DMR Network
      %i.bi.bi-chevron-down.accordion-chevron.text-gray-400.open{id: "chevron-dmr"}
    .accordion-body.px-4.open{id: "body-dmr"}
      .mb-4
        %label.block.text-sm.text-gray-500.mb-1 PROTOCOL
        = f.select :dmr_protocol, [["MMDVM Homebrew (repeater)", "homebrew"], ["Open DMR Terminal / STFU (BrandMeister bridge)", "odmrtp"]], { selected: @bridge.dmr_protocol_or_default }, { id: "dmr_protocol_select", class: "w-full px-3 py-2 rounded-md border border-gray-600 text-sm bg-[#0d1117] text-ghtext" }
        %p.text-xs.text-ghmuted.mt-1
          MMDVM Homebrew logs in as a repeater (HBlink, TGIF, DMR+, IPSC2).
          %strong BrandMeister
          requires Open DMR Terminal (ODMRTP/STFU) for bridge connections — authenticate with your self-care hotspot password.
      .grid.grid-cols-1.md:grid-cols-2.gap-4
        .mb-1
          %label.block.text-sm.text-gray-500.mb-1 HOST
          = f.text_field :dmr_host, class: "w-full px-3 py-2 rounded-md border border-gray-600 text-sm bg-[#0d1117] text-ghtext", placeholder: "e.g. master.brandmeister.network"
          %p.text-xs.text-ghmuted.mt-1 DMR master server hostname or IP
        .mb-1
          %label.block.text-sm.text-gray-500.mb-1 PORT
          = f.number_field :dmr_port, class: "w-full px-3 py-2 rounded-md border border-gray-600 text-sm bg-[#0d1117] text-ghtext", placeholder: "62030"
          %p.text-xs.text-ghmuted.mt-1 Leave blank for the default (Homebrew 62030, ODMRTP 54006)
        .mb-1
          %label.block.text-sm.text-gray-500.mb-1 DMR ID
          = f.number_field :dmr_id, class: "w-full px-3 py-2 rounded-md border border-gray-600 text-sm bg-[#0d1117] text-ghtext", placeholder: "e.g. 1234567"
          %p.text-xs.text-ghmuted.mt-1 Your registered DMR ID (7 digits)
        .mb-1
          %label.block.text-sm.text-gray-500.mb-1 PASSWORD
          = f.text_field :dmr_password, class: "w-full px-3 py-2 rounded-md border border-gray-600 text-sm bg-[#0d1117] text-ghtext", placeholder: "Hotspot security password"
          %p.text-xs.text-ghmuted.mt-1 Hotspot password from the DMR network
        .mb-1
          %label.block.text-sm.text-gray-500.mb-1 TALKGROUP
          = f.number_field :dmr_talkgroup, class: "w-full px-3 py-2 rounded-md border border-gray-600 text-sm bg-[#0d1117] text-ghtext", placeholder: "e.g. 310"
          %p.text-xs.text-ghmuted.mt-1 DMR talkgroup to bridge (e.g. 9990 for parrot)
        .mb-1.dmr-repeater-only
          %label.block.text-sm.text-gray-500.mb-1 TIMESLOT
          = f.select :dmr_timeslot, [[1, 1], [2, 2]], {}, class: "w-full px-3 py-2 rounded-md border border-gray-600 text-sm bg-[#0d1117] text-ghtext"
          %p.text-xs.text-ghmuted.mt-1 DMR timeslot (1 or 2) — Homebrew only
        .mb-1.dmr-repeater-only
          %label.block.text-sm.text-gray-500.mb-1 COLOR CODE
          = f.select :dmr_color_code, (0..15).map { |c| [c, c] }, {}, class: "w-full px-3 py-2 rounded-md border border-gray-600 text-sm bg-[#0d1117] text-ghtext"
          %p.text-xs.text-ghmuted.mt-1 DMR color code (0-15, default 1) — Homebrew only
        .mb-1
          %label.block.text-sm.text-gray-500.mb-1 CALLSIGN
          = f.text_field :dmr_callsign, class: "w-full px-3 py-2 rounded-md border border-gray-600 text-sm bg-[#0d1117] text-ghtext uppercase", placeholder: "e.g. F1ABC", style: "text-transform: uppercase"
          %p.text-xs.text-ghmuted.mt-1 Callsign reported to DMR network (defaults to local callsign)
        .mb-1
          %label.block.text-sm.text-gray-500.mb-1 OPTIONS STRING
          = f.text_field :dmr_options, class: "w-full px-3 py-2 rounded-md border border-gray-600 text-sm bg-[#0d1117] text-ghtext font-mono", placeholder: "e.g. TS2=22270;TIMER=0"
          %p.text-xs.text-ghmuted.mt-1 Optional RPTO options string sent after config (e.g. ADN-style static TG pinning). Leave empty to skip.
        .grid.grid-cols-2.gap-4.mb-1
          .mb-1
            %label.block.text-sm.text-gray-500.mb-1 RX FREQ (Hz)
            = f.text_field :dmr_rx_freq, class: "w-full px-3 py-2 rounded-md border border-gray-600 text-sm bg-[#0d1117] text-ghtext font-mono", placeholder: "e.g. 439987500"
            %p.text-xs.text-ghmuted.mt-1 9-digit RX frequency in Hz, some masters (e.g. TGIF) show/require this. Leave empty for 000000000.
          .mb-1
            %label.block.text-sm.text-gray-500.mb-1 TX FREQ (Hz)
            = f.text_field :dmr_tx_freq, class: "w-full px-3 py-2 rounded-md border border-gray-600 text-sm bg-[#0d1117] text-ghtext font-mono", placeholder: "e.g. 439987500"
            %p.text-xs.text-ghmuted.mt-1 9-digit TX frequency in Hz. Leave empty for 000000000.

  -# ── Local Reflector ──
  .rounded-lg.border.border-gray-600.bg-ghcard.mb-4
    .px-4.py-3.border-b.border-gray-600.flex.items-center.justify-between.cursor-pointer{onclick: "toggleSection('local')"}
      %h5.text-lg.font-semibold.text-gray-100
        %i.bi.bi-broadcast.mr-2
        Local SVXReflector
      %i.bi.bi-chevron-down.accordion-chevron.text-gray-400.open{id: "chevron-local"}
    .accordion-body.px-4.open{id: "body-local"}
      .grid.grid-cols-1.md:grid-cols-2.gap-4
        .mb-1
          %label.block.text-sm.text-gray-500.mb-1 HOST
          = f.text_field :local_host, class: "w-full px-3 py-2 rounded-md border border-gray-600 text-sm bg-[#0d1117] text-ghtext", placeholder: "svxreflector"
        .mb-1
          %label.block.text-sm.text-gray-500.mb-1 PORT
          = f.number_field :local_port, class: "w-full px-3 py-2 rounded-md border border-gray-600 text-sm bg-[#0d1117] text-ghtext", placeholder: "5300"
        .mb-1
          %label.block.text-sm.text-gray-500.mb-1 CALLSIGN
          = f.text_field :local_callsign, class: "w-full px-3 py-2 rounded-md border border-gray-600 text-sm bg-[#0d1117] text-ghtext", placeholder: "e.g. F4ABC-DMR"
          %p.text-xs.text-ghmuted.mt-1 Callsign for the bridge on the local reflector
        .mb-1
          %label.block.text-sm.text-gray-500.mb-1 AUTH_KEY
          = f.text_field :local_auth_key, class: "w-full px-3 py-2 rounded-md border border-gray-600 text-sm bg-[#0d1117] text-ghtext", placeholder: "Shared secret"
        .mb-1
          %label.block.text-sm.text-gray-500.mb-1 TALKGROUP
          = f.number_field :local_default_tg, class: "w-full px-3 py-2 rounded-md border border-gray-600 text-sm bg-[#0d1117] text-ghtext", placeholder: "1"
          %p.text-xs.text-ghmuted.mt-1 TG to bridge to the DMR talkgroup

  -# ── Audio AGC ──
  = render 'form_agc', f: f

  -# Help popup modal
  #help-modal.fixed.inset-0.z-50.items-center.justify-center.hidden{style: "background: rgba(0,0,0,0.6)"}
    .bg-ghcard.border.border-gray-600.rounded-lg.p-5.max-w-md.w-full.mx-4.shadow-xl
      .flex.items-center.justify-between.mb-3
        %h6.text-base.font-semibold.text-gray-100#help-modal-title
        %button.text-gray-400.hover:text-gray-200.cursor-pointer{type: "button", onclick: "closeHelp()"}
          %i.bi.bi-x-lg
      %p.text-sm.text-gray-300.leading-relaxed#help-modal-body

  -# ── Save ──
  .flex.items-center.justify-between
    = link_to admin_bridges_path, class: "px-4 py-2 rounded-md border border-gray-600 text-sm text-gray-300 hover:text-gray-100" do
      %i.bi.bi-arrow-left.mr-1
      Cancel
    = f.submit(@bridge.persisted? ? "Update Bridge" : "Create Bridge", class: "px-4 py-2 rounded-md bg-blue-600 hover:bg-blue-700 text-white text-sm font-semibold cursor-pointer")

:css
  .accordion-body { max-height: 0; overflow: hidden; padding-top: 0; padding-bottom: 0; transition: max-height 0.3s ease, padding 0.3s ease; }
  .accordion-body.open { max-height: 5000px; padding-top: 1rem; padding-bottom: 1rem; }
  .accordion-chevron { transition: transform 0.3s ease; }
  .accordion-chevron.open { transform: rotate(180deg); }

:javascript
  function openModal(id) { var m = document.getElementById(id); m.classList.remove('hidden'); m.classList.add('flex'); }
  function closeModal(id) { var m = document.getElementById(id); m.classList.add('hidden'); m.classList.remove('flex'); }

  function showHelp(key) {
    document.getElementById('help-modal-title').textContent = key;
    document.getElementById('help-modal-body').textContent = (typeof HELP_TEXTS !== 'undefined' && HELP_TEXTS[key]) || 'No description available.';
    openModal('help-modal');
  }
  function closeHelp() { closeModal('help-modal'); }
  document.getElementById('help-modal').addEventListener('click', function(e) { if (e.target === this) closeHelp(); });

  function toggleSection(id) {
    var body = document.getElementById('body-' + id);
    var chevron = document.getElementById('chevron-' + id);
    body.classList.toggle('open');
    chevron.classList.toggle('open');
  }

  // Hide repeater-only fields (timeslot, color code) when using ODMRTP/STFU.
  function syncDmrProtocol() {
    var sel = document.getElementById('dmr_protocol_select');
    if (!sel) return;
    var odmrtp = (sel.value === 'odmrtp' || sel.value === 'stfu');
    document.querySelectorAll('.dmr-repeater-only').forEach(function(el) {
      el.style.display = odmrtp ? 'none' : '';
    });
  }
  (function() {
    var sel = document.getElementById('dmr_protocol_select');
    if (sel) { sel.addEventListener('change', syncDmrProtocol); syncDmrProtocol(); }
  })();

- if @bridge.persisted?
  :javascript
    (function() {
      var inp = document.querySelector('input[name="bridge[name]"]');
      if (!inp) return;
      var orig = inp.dataset.original;
      inp.addEventListener('input', function() {
        document.getElementById('name-warn').classList.toggle('hidden', inp.value === orig);
      });
    })();
